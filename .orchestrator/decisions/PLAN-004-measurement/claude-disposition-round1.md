# Claude disposition — SOL critique round 1 of PLAN-004 rev2 (VERDICT: BLOCK)

Tier: Standard (BLOCK → revise + resubmit). All 12 findings + the architecture reframing are
dispositioned below. Where scaled, the invariant is kept and the residual is recorded in the plan.

| Finding | Disposition | Notes |
|---|---|---|
| Arch: ledger as "canonical evidence" | SUSTAIN | Adopt the reframing verbatim: attempt artifacts stay canonical facts; the ledger is a canonical append-only OBSERVATION JOURNAL; events bind source-set/extractor/schema/boundary digests; head checkpoints; hash-linked generations on corruption. |
| F1 dispatch capture on critical path | SUSTAIN, scaled | Dispatch-side capture = fixed-size nonblocking datagram emission to journald (sd_journal_sendv / /dev/log with MSG_DONTWAIT semantics) — the box's existing quota-bounded logging service; NO regular-file writes, NO fsync, NO wrapper inside dispatch.py. A dropped datagram = coverage gap, never a dispatch effect. The collector materializes journal records post-hoc (journalctl read, operator-side). This also honors the accepted draft's "systemd journals are the durable mechanism". Claude envelope capture moves fully post-hoc (P2-03 reads archived review artifacts + journal). Fault tests per SOL's list (socket absent, buffer full, EINTR, fd exhaustion, cancellation). |
| F2 event identity incompleteness | SUSTAIN | Add source_set_sha256 (all contributing sources), extractor_contract_sha256, config + boundary digests to identity; one event per capture/disposition record OR explicit latest-snapshot supersession per family; fixtures for companion-artifact drift. |
| F3 epoch inference | SUSTAIN | Epoch = post-Phase-1 only via captured harness-commit binding to the pinned Phase 1 set; else `unknown`; trends and A/B mechanically reject unknown/mixed. |
| F4 PLAN-003 contract pinning | SUSTAIN | P2-01 consumes a committed, generated Phase 1 extractor contract (signatures, output schemas, complete status/error-class tables, schema digests, exhaustive enum tests); digests verified every run; unsupported value = measurement_error. |
| F5 report/collector injection surface | SUSTAIN | Base64-embed report data in an inert element (kills </script>); bounds at every layer; non-UTF-8 git path policy (lossless encode or explicit projection failure); dirfd/openat containment for --attempt; URL-scheme rejection; hostile fixtures incl. U+2028/2029, huge arrays, non-UTF-8 names. |
| F6 ledger truncation/repair | SUSTAIN | No in-place repair ever: seal damaged generation (hash preserved), new generation with predecessor hash; periodic head checkpoints stored in the tracked private repo; crash tests at byte boundaries. |
| F7 golden isolation declarative | SUSTAIN | Sealed eval profile REUSES the existing D5 worker boundary (systemd-run codex-worker, empty HOME, scrubbed env, no agent/credential fds, no production mounts, network per existing worker policy) for BOTH worker and reviewer under test; bundle ref/object closure validation (no oracle/hidden/label blobs); production-side-effect canaries fail closed pre-invocation. |
| F8 dataset digest + holdout | SUSTAIN, scaled | Merkle-style dataset digest over manifests/specs/fixture closures/modes/oracles/hidden checks/scorer digest/ordered list; real rubric criteria (example marked non-normative); operator-owned holdout registry + rotation epoch + routine-mode rejection. SCALED: holdout task bodies live in the PRIVATE repo (orchestrator-private) rather than a new sealing mechanism; residual recorded that public-task inspection by harness authors remains possible. |
| F9 rubric-oracle binding | SUSTAIN | Enumerated assertion kinds with trusted evidence sources per bucket; reviewer defect-catch requires structured affected-path/rule/outcome match, never keyword presence. |
| F10 reviewer-value experiment manifest | SUSTAIN | Freeze ONE worker artifact digest; arms fork only after it; arm manifests record intervention/evaluator digest/outcomes; audit-action-7 consumer added to P2-07 allowed paths with a resolution test. |
| F11 statistical honesty | SUSTAIN | Raw paired differences + descriptive range for n=2–3 (no "confidence interval" label); predeclared hierarchical resampling for aggregates (task→paired repeat, fixed seed/draws/complete-case rules); paired_n + one-sided-missing accounting; intent-to-treat for scheduled failures; delta_percent null below declared thresholds and for bounded metrics; deterministic AB/BA counterbalancing; trusted runner/scorer/oracle frozen OUTSIDE both compared commits; full causal-config hash comparison; run-count arithmetic fixed and holdout IDs recorded. |
| F12 ordering/fresh-box/repro | SUSTAIN | P2-03 (wrapper/envelope interface + raw schemas) reordered BEFORE P2-02; raw-capture schemas owned by P2-01; run-matrix writes finalized artifacts only (collector projects them — never appends the ledger); `measure bootstrap`/`doctor` defined for fresh clones; report as_of derives from ledger head unless supplied; canonical JSON (sorted keys, fixed float repr) specified; generated_at only in non-canonical HTML metadata. |

Residuals to record in the plan: journald capture depends on journald availability/quota (absence =
coverage gap, never dispatch impact); public-task inspectability (holdout bodies private, rest
public); no cryptographic signing of checkpoints (tracked-repo integrity only, cross-ref
evidence-hashing plan).
