Reading additional input from stdin...
OpenAI Codex v0.144.3
--------
workdir: /home/val/orchestrator
model: gpt-5.6-sol
provider: openai
approval: never
sandbox: read-only
reasoning effort: high
reasoning summaries: none
session id: 019f5f34-3113-75e2-8d14-fcab21788daa
--------
user
You are SOL (gpt-5.6), the DRAFTER of PLAN-003. An independent fresh-context SOL design review returned VERDICT: BLOCK with findings B1-B12 plus operational/assumption/validation defects (full critique below). Claude has dispositioned every finding (disposition table below) — most SUSTAINED, several deliberately SCALED with the invariant kept and the residual recorded.

Produce PLAN-003 REVISION 3 in full. Rules:
- Address every SUSTAINED/SCALED disposition exactly as scoped by Claude's disposition table — where Claude scaled an amendment, implement the scaled form and add the residual to a new "## Residual risks (recorded)" section; do not silently re-expand or re-shrink scope.
- Key structural changes you must make: add Dispatch 0 (operator-installed bootstrap verifier + activation manifest + /srv/codexwork/installed/<commit> trusted snapshot provisioning); per-required-test expected-assertion-ID sets with exact-set matching; producer-authenticated events (env scrub + FD close before candidate exec, per-invocation nonces, separate trusted/supplemental pipes, controller-only event production); tree revalidation before review and before PR; venv address includes python ABI/platform + python -I + shadow-module tests; duplicate-JSON-key rejection + launch-time persisted criteria vector; machine-checked activation manifest; three-state compatibility sequence; B11 routing decision (pick (a) dependency on the approval-schema plan or (b) explicit weakening — state which and why); B6 scoped to O_EXCL no-replace for NEW artifacts + dispatcher-owned paths, cross-referencing the separate evidence-hashing plan; Phase 2 gate = all five (now six) dispatches landed.
- Fold the critique's 18 missing acceptance tests and the falsely-green mitigations into numbered acceptance criteria (SPEC-015 replay must run through the REAL gate state machine with reviewer/PR spies; parity baselines captured pre-candidate and approval-bound; metrics window anchoring defined).
- Keep everything already correct in rev2 (parent-blob execution, supplemental namespace concept, positional v4 binding, structured blocker predicate, metrics extraction/semantics split, PLAN-TEMPLATE structure, frontmatter id PLAN-003). Increment frontmatter to revision: 3.
- End with "## Disposition record (revision 3)" mapping each finding B1-B12 + operational items to what changed (or, for the two PARTIAL/routing items, what was recorded instead).
- You have read-only repo access; verify any new path:line citations.
---
id: PLAN-003
created: 2026-07-14T05:42:58Z
author_model: gpt-5.6-sol
status: challenged
task: "PHASE-1 PLAN (verdict integrity + metrics semantics) — redraft to the PLAN-TEMPLATE.md standard"
ledger_ref: R19
lane: mixed
supersedes: drafts/verdict-integrity/PLAN-001
revision: 2
---

# PLAN-003 — Phase 1 verdict integrity and metrics semantics

## 1. Decision & non-goals

Phase 1 will make test execution and binary review verdicts mechanically trustworthy before any candidate can advance to PR creation. The installed parent dispatcher—not the candidate’s dispatcher—will select required tests from the actual base-to-candidate diff. Required trust-suite tests will execute from content bound to the installed parent commit while targeting the candidate worktree; candidate-modified test copies may run additionally but can never satisfy the parent-required attestation. The dispatcher will stop at the test gate unless every required trusted test executed at least one assertion and passed.

Review verdict schema v4 will bind one ordered verdict entry to every acceptance criterion and represent scope, regression, and security findings with structured severities that mechanically block a PASS. The existing shared gate-venv and sanitized-reviewer-log design is retained.

Metrics work will first extract reporting from the trust-critical dispatcher through a behavior-preserving high-assurance change. A following ordinary-risk reporting change will remove unsupported direct-merge attribution and define straight-through rate per completed spec rather than per attempt. The ordinary-risk metrics dispatch will have no write scope over `scripts/dispatch.py`. Corrected metrics semantics remain the prerequisite for Phase 2’s measurement layer.

Assertion-event migration across the existing shell suite will be a separate high-assurance predecessor. This keeps the homogeneous mechanical migration independently reviewable before the dispatcher, runner, isolation, policy, and reviewer-evidence changes are reviewed.

Non-goals:

- Do not change the worker model, reasoning depth, priority service tier, reviewer seat, branch topology, or human merge authority.
- Do not permit the candidate checkout to run its own gate implementation or supply the authoritative copy of a required trust-suite test. Candidate code may be the subject of tests, but the dispatcher, required-test content, test policy, attestation aggregator, and gate-transition logic must come from the installed parent version recorded in the attempt manifest.
- Do not repair unrelated isolation, stale-base, manual-merge, resource-ceiling, approval-binding, or evidence-retention findings.
- Do not infer whether an unmatched commit was written or merged by a human, operator, bot, or external automation.
- Do not turn Phase 1 metrics into a broader analytics or measurement layer. Phase 2 remains gated on the narrowly corrected metrics semantics.
- Do not make reviewer judgment authoritative for whether tests executed, whether criteria are complete, whether required test content was trusted, or whether a finding blocks.
- Do not parse human-readable `SKIP`, `PASS`, or assertion text from `test.log` as authorization input.
- Do not expose the operator’s home or `.venv`, install packages from the candidate checkout, enable network access in the isolated test unit, or install dependencies as `codex-worker`.
- Do not alter, replace, or append to `.orchestrator/attempts/SPEC-015/1/*`. Historical evidence is immutable.
- Do not retroactively convert legacy verdicts to v4. Legacy artifacts remain displayable but cannot authorize a new or resumed gate decision.
- Do not redesign the spec schema beyond any mechanical compatibility change strictly required to name test IDs. Required-test selection remains a parent-owned policy derived from changed paths.
- Do not authorize an incompatible test-semantics change merely because the candidate supplies a replacement test. Such a change must use the compatibility sequence or separately authorized bootstrap procedure defined in §4.4.

## 2. Current-state evidence

### 2.1 Observed facts

1. The current test gate writes `test.log` and uses the child process return code as its success signal; it does not validate which tests or assertions executed (`scripts/dispatch.py:935-951`).

2. The repository test wrapper treats any child exit code of zero as `PASS`; it has no separate successful-skip state (`scripts/test:22`). That is why a test can print `SKIP`, exit zero, and immediately be reported as passing.

3. The recorded SPEC-015 run proves this is not hypothetical:

   - `dispatch_gate4.sh` skipped because PyYAML/jsonschema were absent (`.orchestrator/attempts/SPEC-015/1/test.log:7`).
   - `dispatch_parallel.sh` skipped for the same reason (`.orchestrator/attempts/SPEC-015/1/test.log:10`).
   - `worker_isolation.sh` skipped because its on-box prerequisites were absent (`.orchestrator/attempts/SPEC-015/1/test.log:30`).
   - These tests correspond to live trust-boundary test surfaces in `tests/dispatch_gate4.sh`, `tests/dispatch_parallel.sh`, and `tests/worker_isolation.sh`; their current prerequisite branches return successful skip rather than a gate failure.

4. The reviewer received only the statement that `./scripts/test` exited zero (`scripts/dispatch.py:1186-1188`; `.orchestrator/attempts/SPEC-015/1/raw/review-request.txt:54-57`). Reviewer filesystem tools were denied, so it could not read the omitted test log (`scripts/dispatch.py:1191-1200`).

5. Despite those skips, the stored verdict marked all seven criteria `MET` and claimed dynamic test proof that did not exist (`.orchestrator/attempts/SPEC-015/1/review.json:5,10,15,20,25,30,35`). This is the required historical regression case.

6. The current verdict schema requires only one `criteria[]` item. “One entry per acceptance criterion” is descriptive text rather than an enforced spec-dependent constraint (`scripts/verdict.schema.json:8-11`).

7. `evaluate_binary_review()` currently examines only the criterion entries the reviewer supplied and accepts a PASS when all supplied entries are `MET`; it does not establish that the list is complete or bound to the spec (`scripts/dispatch.py:1106-1116`).

8. Review validation currently binds `spec_digest`, `base_sha`, and `worker_commit`, which proves artifact identity but not criterion completeness or evidentiary truth (`scripts/dispatch.py:1119-1134`).

9. Scope, regression, and security findings are presently required as nonempty prose but do not mechanically affect a PASS. A verdict can therefore describe a critical finding and still be accepted by `evaluate_binary_review()` (`scripts/dispatch.py:1106-1116`).

10. The worker-controlled diff is read and inserted into the reviewer prompt (`scripts/dispatch.py:1141,1188`). This is an untrusted prompt channel even though no prompt injection was identified in SPEC-015.

11. The CI contract is the single non-matrix job named `ci` in `.github/workflows/ci.yml`. It runs the repository entry point but does not itself cure a successful prerequisite skip; the new attestation semantics must therefore be exercised in CI rather than relying on the workflow’s aggregate exit status.

12. The reporting code currently gives the complement of orchestrator-attributed merges a direct-merge actor attribution in `scripts/delegation_report.py` even though the available evidence establishes only that those commits were not matched to an orchestrator record. The same reporting surface’s `dispatch metrics` calculation treats attempts as the rate population, allowing retries to change both numerator and denominator instead of measuring completed specs. These are semantic reporting defects, not gate-state defects.

13. `scripts/dispatch.py` is in the dispatcher trust closure. A path-based scope gate cannot mechanically constrain an ordinary-risk worker to only its metrics functions. Therefore the reporting logic must be extracted through a separately authorized high-assurance change before ordinary-risk metrics semantics can be changed safely.

### 2.2 Assumptions

1. `scripts/requirements.txt` remains the trusted source of pinned Python dependencies. This plan does not alter dependency versions.

2. The operator can provision `/srv/codexwork/venvs/gate/<requirements-sha256>` through the existing privileged setup procedure before activating the repaired test gate.

3. The installed parent checkout and its executable path can be recorded in each attempt manifest. If they cannot be resolved to a trusted commit, dispatch fails closed.

4. Existing shell tests can be migrated mechanically to a shared assertion helper without changing what they test. This assumption must be demonstrated through the mutation drill in R1 and §8.4; it is not accepted merely because the migrated suite remains green.

5. A machine can prove that trusted test content ran and emitted assertion results; it cannot prove that every assertion is semantically sufficient. Parent-owned test selection and content, migration-fidelity mutation drills, scope review, bound review, and human authorization remain necessary.

6. Historical raw logs may be unavailable in a clean CI checkout because raw attempt evidence is gitignored. Therefore validation uses both an on-box read-only replay of the real SPEC-015 evidence and a tracked synthetic fixture containing the same missing-attestation/skip pattern. The fixture is regression input, not replacement historical evidence.

7. Required trust tests can be adapted during the migration predecessor to distinguish their trusted source root from the candidate target root. A test that cannot make that distinction is not eligible to authorize a candidate until its adapter is corrected and installed as parent content.

8. A candidate test change that intentionally contradicts an installed parent test cannot be authorized by substituting the candidate copy. The normal route is the two-stage compatibility sequence in §4.4. An exceptional incompatible bootstrap requires authority outside this plan.

9. Any drift in the cited symbols before authorization requires refreshing citations and re-running challenge. It does not reopen the decisions in this plan.

## 3. Requirements & acceptance criteria

### R1. Machine-enforced, parent-content-bound required-test attestation

The parent test gate must possess an exact, parent-selected set of required test IDs. The authoritative content for every required trust-suite ID must come from the installed parent commit recorded in the attempt manifest, not from the candidate worktree. Each trusted test must target the candidate checkout through the parent-owned target-root interface.

Each required ID must be launched exactly once from its trusted source, exit zero, emit at least one valid passing assertion event, emit no failing assertion event, and finish with status `PASS`. A required `SKIP`, absent trusted test, duplicate result, zero-assertion result, malformed event, stale nonce, nonzero exit, missing source binding, source-blob mismatch, target-root mismatch, or missing aggregate attestation must produce `failed_test` before reviewer invocation. Aggregate command exit zero is never sufficient.

Candidate-modified copies of required tests must be detected from the base-to-candidate diff and run as supplemental tests. They may add evidence and may fail the gate, but their assertions never count toward the parent-required set and cannot replace or suppress a trusted result.

Acceptance criteria:

1. A fixture in which one required trusted test prints `SKIP` and exits zero ends as `failed_test`; review and PR-opening spies have zero calls.
2. A required trusted test that exits zero without emitting an assertion event ends as `failed_test` with reason `zero_assertions`.
3. Missing, duplicated, unknown, escaping, symlinked, non-regular, or parent-commit-mismatched required test IDs fail closed.
4. A result with a stale run nonce, wrong base/head binding, wrong parent source commit, wrong trusted blob hash, wrong target root, duplicate sequence, unknown status, or overall `PASS` inconsistent with a child result fails closed.
5. A candidate touching `scripts/dispatch.py` and declaring `test_command: true` still runs the parent-selected, parent-content-bound trust suite against the candidate tree; the declared command cannot satisfy or replace required IDs.
6. Every required ID appears exactly once in the persisted trusted attestation with its parent commit, trusted blob hash, candidate target commit, positive assertion count, zero child exit, and `PASS` before review can begin.
7. The actual `.orchestrator/attempts/SPEC-015/1/test.log`, supplied without a valid structured attestation, replays to a blocking `legacy_missing_attestation` result without modifying any SPEC-015 file.
8. Replacing a candidate copy of a required test with a stub that reads the injected environment, emits well-formed passing events, and exits zero does not satisfy the required result. The trusted parent copy still runs, and the persisted aggregate separates `trusted_required_results` from `candidate_supplemental_results`.
9. Deleting or renaming the candidate copy of a required test does not remove the trusted execution. The deletion or rename is recorded as a changed-test condition and processed under §4.4.
10. For every assertion-helper form introduced by the migration, and for at least one migrated test representing each distinct shell assertion idiom, an induced failure of the guarded condition must emit `ok: false` and exit nonzero. No tested helper form may emit `ok: true` before its condition is evaluated.
11. A candidate-added required-test proposal cannot become authoritative during its own attempt. It is supplemental until merged, installed, and rerun from the newly installed parent during the activation drill.

### R2. Verdict schema v4 criterion count and identity binding

Verdict schema v4 must require `schema_version: 4` and structured `criteria[]` entries. Dispatcher-side validation must require the criteria list to have exactly the same length and order as the bound spec’s `acceptance_criteria`. Every entry must carry its one-based `criterion_index`, and its criterion text must match the corresponding spec criterion exactly or under the normalization defined in §4.6. Missing, extra, reordered, duplicated, or mismatched criteria reject the verdict before binary evaluation.

Acceptance criteria:

1. For an N-criterion spec, only a verdict with exactly N criteria entries can pass.
2. Omitting one entry, adding one entry, duplicating an index, reordering entries, using a zero- or out-of-range index, or substituting invented criterion text rejects the verdict fail-closed.
3. Exact text and the explicitly defined whitespace/Unicode-normalized equivalent pass binding; case, punctuation, Markdown, or wording changes do not.
4. A schema-valid PASS with every supplied criterion `MET` still fails if criterion count or binding is wrong.
5. Legacy v1-v3 artifacts remain readable for display but are never accepted for a new or resumed review decision.

### R3. Structured blocking findings

Schema v4 must replace prose-only scope, regression, and security finding fields with arrays of structured finding objects. Each finding requires `severity`, `summary`, and `evidence`; allowed severities are exactly `advisory` and `blocker`. An empty array means no finding. `evaluate_binary_review()` must return failure if any of the three arrays contains `severity: blocker`. Missing arrays, non-arrays, malformed entries, absent or unknown severity, or evaluation exceptions are treated as blockers even if upstream schema validation was accidentally bypassed.

Acceptance criteria:

1. A verdict with all criteria `MET` and one blocker in any of `scope_findings`, `regression_findings`, or `security_findings` is rejected.
2. An all-`MET` PASS with empty finding arrays or advisory-only findings may pass if every other binding succeeds.
3. `critical`, `high`, empty, null, misspelled, numeric, or otherwise unknown severity values are rejected; no substring search or severity inference is used.
4. Direct unit tests of `evaluate_binary_review()` prove that malformed findings fail closed even when called without prior schema validation.
5. The reviewer prompt and schema examples use the same structured vocabulary; no production branch continues accepting prose-only finding strings.

### R4. Shared isolated gate environment

Tests requiring PyYAML/jsonschema must run using a content-addressed virtual environment derived from the trusted parent checkout’s `scripts/requirements.txt`. It must be readable and executable but not writable by `codex-worker`. Missing, stale, mismatched, writable, or unusable environments block the test gate without falling back to operator-home, system packages, network installation, or unisolated execution.

Acceptance criteria:

1. The exact isolated on-box test unit imports `yaml` and `jsonschema` through `/srv/codexwork/venvs/gate/<digest>` and executes the required dispatcher assertions.
2. As `codex-worker`, imports succeed but create, replace, remove, rename, and chmod operations inside the shared venv fail.
3. A missing or mismatched digest marker blocks before any required test and before review.
4. A candidate change to `scripts/requirements.txt` blocks until the operator provisions the matching trusted-parent digest; candidate-authored requirements are never installed during the gate.
5. No production or test path references `/home/val/.venv`, performs `pip install` as `codex-worker`, enables network access, or falls back to an unisolated command.

### R5. Reviewer receives bounded, sanitized test evidence

After R1 succeeds, the reviewer request must include the trusted structured attestation plus a bounded, sanitized tail of `test.log`. The log remains explicitly untrusted data. Missing, symlinked, changing, oversized-beyond-policy, or unreadable evidence blocks review rather than silently degrading to an exit-code summary.

Acceptance criteria:

1. The reviewer request identifies every required test, its trusted source commit and blob hash, candidate target commit, assertion count and PASS status, the full-log hash, byte counts, line counts, and truncation status.
2. A recognizable final test-log line is present as JSON-escaped evidence; the request contains more than “exited 0.”
3. ANSI controls, NUL/C0/C1 controls, invalid UTF-8, delimiter text, Markdown fences, and instruction-like text cannot alter fixed reviewer instructions or create a closing delimiter.
4. Only the final 16 KiB and final 80 logical lines are supplied; the complete raw log remains unchanged.
5. Test-log bytes never appear in process arguments or shell interpolation.
6. Deleting, replacing, symlinking, or racing the log between validation and prompt construction blocks review.

### R6. Correct metrics semantics

The reporting-only metrics implementation in `scripts/metrics_report.py` must rename every actor-attributing direct-merge label to `not_attributed_to_orchestrator`, with explanatory text that actor identity is unknown. The metrics command must calculate straight-through rate per eligible completed spec:

- Denominator: distinct specs with a terminal dispatch result in the selected corpus/window.
- Numerator: denominator specs whose first and only attempt reached the normal orchestrator `merged` terminal state after all gates, with no retry, rebase replacement, manual override, or later attempt.
- Each spec contributes at most once.
- In-progress or structurally incomplete specs are excluded and separately counted.
- A zero denominator produces JSON `null` and display `N/A`, not `0%` or `100%`.

Acceptance criteria:

1. No output, JSON key, heading, or help text attributes an unmatched merge to a human or operator.
2. The replacement label is `not_attributed_to_orchestrator` and explains only the mechanically supported fact.
3. A committed fixture containing one first-attempt merge, one retry-then-merge, one terminal first-attempt failure, one in-progress spec, and one malformed/incomplete record produces numerator `1`, denominator `3`, rate `1/3`, excluded-in-progress `1`, and malformed/incomplete `1`.
4. Adding attempts to the retrying spec does not change the denominator or numerator.
5. Empty eligible input returns `null`/`N/A` without division errors.
6. Existing non-rate counts remain unchanged unless their labels asserted unsupported actor attribution.
7. Phase 2 measurement work cannot dispatch until the ordinary-risk metrics semantics dispatch is merged and its fixture passes on `integration`.
8. The ordinary-risk metrics semantics dispatch has no allowed path in the dispatcher trust closure and cannot modify `scripts/dispatch.py`, `scripts/dispatch`, gate transitions, attempt serialization, or authorization logic.
9. The preceding high-assurance extraction preserves the existing metrics behavior byte-for-byte or field-for-field, except for unavoidable module-origin metadata explicitly enumerated in its spec.
10. Non-metrics dispatcher commands neither import nor depend on `scripts/metrics_report.py`; failure to import the reporting module cannot alter a gate command’s behavior.

### R7. Bootstrap and evidence invariants

Every spec in this plan must be gated by the installed parent dispatcher recorded before candidate work begins. The candidate’s `scripts/dispatch.py`, test aggregator, policy, schema validator, evaluator, or required-test copy must never be selected as its own gate implementation or authoritative trust test. Attempts and raw evidence remain append-only/immutable.

Acceptance criteria:

1. The attempt manifest records the installed parent executable realpath and parent commit.
2. A candidate dispatcher replaced with a marker-writing stub is not invoked by the gate; the marker remains absent.
3. Gate commands use absolute parent-controlled executable, policy, runner, helper, and trusted-test paths while targeting the candidate worktree as data.
4. SPEC-015 evidence hashes and bytes are unchanged after replay.
5. New failed-test and failed-review artifacts are retained; no rollback or compatibility path rewrites them as passes.
6. Every high-assurance dispatch has the required operator approval artifact bound to its digest and attempt. An approval for one dispatch, attempt, or test-content digest cannot authorize another.
7. Activation of a newly merged parent is conditional on rerunning its newly authoritative tests from that installed parent against the exact merged commit.

## 4. Design / approach

### 4.1 Dispatch decomposition and dependency order

| Order | Dispatch | Lane | Dependency | Purpose |
|---:|---|---|---|---|
| 1 | Metrics extraction | high-assurance | None | Move metrics implementation behind a standalone reporting module while preserving behavior; remove ordinary metrics work from the dispatcher trust closure. |
| 2 | Metrics semantics | ordinary | Dispatch 1 merged and installed | Correct attribution naming and straight-through-rate semantics without allowing writes to `scripts/dispatch.py`; unblock Phase 2. |
| 3 | Assertion-event migration | high-assurance | Dispatch 2 merged | Homogeneously migrate the shell suite to assertion events, explicit exit-77 skips, and target-root separation; prove migration fidelity. |
| 4 | Test attestation, shared venv, and reviewer test evidence | high-assurance | Dispatch 3 merged and installed | Stop skipped, missing, substituted, and zero-assertion tests at the test gate; retain the shared-venv and sanitized-log design. |
| 5 | Verdict schema v4 and blocking-finding evaluation | high-assurance | Dispatch 4 merged, installed as parent, and on-box validation passed | Enforce complete criterion binding and mechanical blocker handling under the repaired parent test gate. |

Dispatch 1 must be reviewed and authorized as a trust-closure change even though it is behavior-preserving. Dispatch 2 is ordinary-risk only because its allowed paths exclude the dispatcher and all other gate components.

Dispatch 3 must be installed before Dispatch 4 begins so Dispatch 4 has parent-owned assertion-capable tests. Dispatch 4 must be installed as the active parent before Dispatch 5 begins. Merging code without the required installation and activation drill does not satisfy either dependency.

### 4.2 Per-spec allowed paths

The authorization artifacts must expand every allowed path to literal repository-relative paths. No generated spec may use a broad `scripts/**`, `tests/**`, or repository-wide allowance.

**Dispatch 1 — metrics extraction:**

- `scripts/dispatch.py`, limited to removing the metrics implementation, adding the lazy thin alias, and no other dispatcher behavior
- `scripts/metrics_report.py` (new)
- `tests/metrics_report.sh` (new)
- `tests/dispatch_gate4.sh`, limited to proving metrics alias compatibility and non-metrics import isolation

This dispatch is high-assurance because `scripts/dispatch.py` is in scope. Its approval artifact must bind the exact attempt and digest.

**Dispatch 2 — metrics semantics:**

- `scripts/metrics_report.py`
- `scripts/delegation_report.py`
- `tests/metrics_report.sh`
- `tests/delegation_report.sh`
- `tests/fixtures/metrics_semantics.json` (new)

`scripts/dispatch.py` and `scripts/dispatch` are explicitly excluded. The installed alias from Dispatch 1 obtains the corrected behavior by calling the standalone module.

**Dispatch 3 — assertion-event migration:**

- `scripts/test`
- `scripts/lib/test_attest.sh` (new)
- `tests/assertion_migration.sh` (new)
- Every other currently tracked `tests/*.sh` that must acquire assertion-event calls, exit-77 conversion, or target-root separation. Before authorization, the generated spec must replace this sentence with the literal output of `git ls-files 'tests/*.sh'`; no non-test path may be added through that expansion.

This dispatch may not change dispatcher gate logic, policy, venv setup, CI, verdict handling, or reviewer construction.

**Dispatch 4 — attestation, venv, and reviewer evidence:**

- `scripts/dispatch.py`, limited to required-test selection, trusted-source execution/result validation, test evidence persistence, isolated-unit construction, and reviewer prompt construction
- `scripts/test`
- `scripts/test_runner.py` (new)
- `scripts/test-policy.json` (new)
- `scripts/setup-worker-user.sh`
- `tests/dispatch_gate4.sh`
- `tests/dispatch_parallel.sh`
- `tests/worker_isolation.sh`
- `tests/test_attestation.sh` (new)
- `tests/reviewer_evidence.sh` (new)
- `tests/fixtures/verdict_integrity/spec015_skips.log` (new synthetic regression fixture)
- `.github/workflows/ci.yml`

Any changed or added test in this dispatch is supplemental during its own candidate attempt and must be named by exact old/new blob identity in the high-assurance approval artifact. It becomes authoritative only after merge, installation, and the activation drill.

**Dispatch 5 — verdict v4 and blockers:**

- `scripts/dispatch.py`, limited to review parsing, v4 binding validation, `evaluate_binary_review()`, and reviewer prompt/schema-version instructions
- `scripts/verdict.schema.json`
- `tests/dispatch_gate4.sh`
- `tests/verdict_integrity.sh` (new)
- `tests/fixtures/verdict_v4.json` (new)

Changes outside the corresponding set are scope failures, not occasions to enlarge the spec during implementation.

### 4.3 Parent-owned required-test policy

`scripts/test-policy.json` is a versioned, parent-owned mapping from changed path classes to exact required test IDs and execution lanes.

The policy must implement these decisions:

1. Classify both old and new paths of a rename from the approved base-to-candidate diff.
2. Changes to dispatcher, verdict schema, spec schema, test runner, test policy, requirements, CI, isolation setup, test helper, or a required test require the full trust suite supported by the current lane.
3. Changes under an unrecognized path in `scripts/`, `tests/`, `specs/`, `.github/workflows/`, or `.orchestrator/` require the full trust suite.
4. Ordinary product/helper paths map to their exact existing regression tests. A changed path with no mapping is a policy error, never “no required tests.”
5. On-box-only tests such as worker isolation are mandatory for a high-assurance dispatch that touches their boundary. CI records them as not runnable in that lane but cannot authorize deployment by itself.
6. Candidate changes to the policy do not affect that candidate’s gate. They become active only after merge, authorization, installation, and parent-version recording.
7. Required IDs resolve against the installed parent checkout and are bound to that commit’s blob identities. The candidate worktree is used only as the target under test.
8. Candidate changes to a required-test path trigger both the trusted parent execution and a separate supplemental candidate-test execution. No candidate result is admitted to the required-result namespace.
9. Candidate-added tests are never silently promoted into the required set. Promotion requires merge, parent installation, policy activation, and the post-install drill.

### 4.4 Trusted test source, candidate target, and legitimate test changes

The runner maintains two distinct roots:

- `trusted_test_root`: the installed parent checkout recorded in the attempt manifest.
- `candidate_target_root`: the candidate worktree at the bound `worker_commit`.

A required trust test’s executable, assertion helper, policy metadata, and trusted fixtures resolve from `trusted_test_root`. Code and data explicitly designated as the system under test resolve from `candidate_target_root`. The runner supplies both roots through a clean parent-constructed environment. Migrated tests must use the parent helper to resolve trusted support files and the target-root helper to resolve candidate paths. A required test that resolves its own script location as the system-under-test root, reads a helper from the candidate tree, or cannot demonstrate the root separation fails with `HARNESS_ERROR`.

Before launch, the runner obtains each required test’s expected Git blob identity from the recorded parent commit and verifies the opened regular file against it. It opens without following symlinks and executes the verified descriptor or an equivalently race-safe path. The attestation records the parent commit, blob identity, trusted source root, candidate commit, and candidate target root.

For a candidate-modified required-test path:

1. The trusted parent copy still executes against the candidate target and supplies the only required result.
2. The candidate copy executes once as a supplemental test in a separately labelled namespace.
3. Supplemental assertion events cannot use, overwrite, duplicate, or satisfy a trusted required ID.
4. A supplemental skip, failure, malformed event, or harness error blocks the attempt; a supplemental PASS adds evidence but does not weaken the trusted result.
5. The high-assurance approval artifact must enumerate each changed test path and its old and new blob identity. An unlisted test delta invalidates approval.
6. After merge and installation, the new parent copy is rerun against the exact merged commit before activation. Only then may its blob become authoritative for later candidates.

A legitimate change that alters test implementation without contradicting the old test may land normally if both trusted and supplemental copies pass.

A legitimate behavior change that contradicts an installed parent test must use a two-stage compatibility sequence:

1. Land an implementation and test update that remains compatible with the installed trusted test.
2. Merge and install that test as new parent content and pass the activation drill.
3. In a following high-assurance dispatch, remove the compatibility behavior while the updated parent test is authoritative.

If no compatibility bridge is technically possible, the normal pipeline fails closed. Proceeding would require a separately scoped operator bootstrap authorization that names the exact retired and replacement test blobs, explains why the old test cannot remain valid, and defines independent validation. This plan does not grant or create that exceptional authority.

### 4.5 Test-attestation protocol

The installed parent `scripts/test_runner.py` performs deterministic orchestration using only the Python standard library. `scripts/test` remains the public entry point.

For every required trusted test, the runner:

1. Resolves the exact repository-relative ID beneath the installed parent checkout without following symlinks.
2. Verifies the test file’s Git blob identity against the recorded parent commit.
3. Creates a per-run cryptographic nonce and a dedicated assertion-event channel.
4. Injects the test ID, nonce, trusted source root, and candidate target root as fixed parent-controlled values.
5. Launches the exact trusted test once using argv, never a generated shell command.
6. Counts JSON-line assertion events emitted through the installed parent’s `scripts/lib/test_attest.sh`.
7. Captures exit status and output independently of those events.
8. Runs any changed candidate copy separately under a supplemental namespace.
9. Writes the aggregate result itself to a parent-created attempt-local regular file using a temporary file, `fsync`, and atomic rename.

An assertion event contains exactly:

```json
{
  "version": 1,
  "run_nonce": "<nonce>",
  "test_id": "tests/example.sh",
  "sequence": 1,
  "event": "assertion",
  "ok": true,
  "name": "short stable assertion name"
}
```

A prerequisite failure emits a `skip` diagnostic event and exits `77`. It never emits a passing assertion. Test helpers must evaluate the guarded condition before emitting an event. Failed assertions emit `ok: false` and then cause a nonzero test exit. The helper API must not expose an unconditional “record pass” operation to migrated tests.

The aggregate attestation contains:

- `schema_version`
- `run_nonce`
- `parent_gate_commit`
- `trusted_test_root`
- `spec_digest`
- `base_sha`
- `worker_commit`
- `candidate_target_root`
- exact ordered `required_test_ids`
- `trusted_required_results`, with parent commit and blob hash per ID
- `candidate_supplemental_results`, separately namespaced
- one result per executed ID with `status`, `exit_code`, and `assertions_executed`
- overall status
- creation timestamp

Per-test statuses are `PASS`, `SKIP`, `FAIL`, or `HARNESS_ERROR`. The test gate has only authorization outcomes `pass` and `failed_test`; a required skip is diagnostic `SKIP` but authorizes nothing.

Supplemental immutable-spec `test_command` execution remains required and any nonzero result fails. It cannot satisfy the parent-required test set. If it calls `scripts/test`, it receives the same source-separated attestation behavior.

### 4.6 Verdict v4 criterion normalization and binding

Schema v4 requires each criterion entry to contain:

- `criterion_index`: one-based integer
- `criterion`: nonempty string
- `result`: `MET` or `UNMET`
- `evidence`: nonempty string

Dispatcher validation compares entry `i` only with spec acceptance criterion `i`; it does not perform unordered or fuzzy matching.

Normalization is exactly:

1. Apply Unicode NFKC.
2. Convert CRLF and CR to LF.
3. Strip leading and trailing Unicode whitespace from the entire string.
4. Replace every remaining maximal Unicode-whitespace run, including newlines, with one ASCII space.

Normalization does not case-fold, remove punctuation, render Markdown, resolve links, reorder clauses, or otherwise rewrite wording. If two spec criteria normalize to the same text, their positions remain distinct and both entries are required.

Validation order is:

1. Size and JSON parse.
2. Schema v4.
3. `spec_digest`, `base_sha`, and `worker_commit` binding.
4. Exact count.
5. Sequential indices.
6. Normalized positional text binding.
7. Binary review evaluation.

Failure at any stage records a non-PASS review outcome and prevents PR creation.

The v4 top-level object, every criterion object, and every finding object use `additionalProperties: false`. There is no extensible v4 authorization object left open to implementer discretion. Legacy display-only readers are separate from the v4 authorization schema and are not changed by this rule.

### 4.7 Structured findings and blocker predicate

Each of the existing finding domains becomes an array:

```json
{
  "scope_findings": [],
  "regression_findings": [
    {
      "severity": "advisory",
      "summary": "Non-blocking observation",
      "evidence": "Bound evidence"
    }
  ],
  "security_findings": [
    {
      "severity": "blocker",
      "summary": "Trust boundary bypass",
      "evidence": "Bound evidence"
    }
  ]
}
```

The blocker predicate is:

```text
blocking =
    findings is not an array
    OR any entry is not an object
    OR any required field is absent or invalid
    OR any undeclared field is present
    OR severity is not exactly "advisory" or "blocker"
    OR any severity is exactly "blocker"
```

An empty array is the only representation of “no findings.” There is no `none`, `clear`, `low`, `medium`, `high`, or `critical` severity. This prevents ambiguous threshold mapping. `evaluate_binary_review()` defensively rechecks the predicate even after schema validation.

A review passes only when:

- the bound top-level verdict is PASS;
- v4 schema and all bindings succeeded;
- every required criterion is present and `MET`;
- all three finding arrays are structurally valid; and
- none contains a blocker.

### 4.8 Shared gate virtual environment

The trusted environment is `/srv/codexwork/venvs/gate/<sha256>`, where the digest is over the installed parent checkout’s `scripts/requirements.txt`.

`scripts/setup-worker-user.sh` must:

1. Build in a sibling temporary directory with restrictive umask.
2. Install exactly the trusted requirements file.
3. Verify imports and pinned direct-package versions.
4. Record digest, Python implementation/version, and verification result in a root-owned marker.
5. Remove group/other write permission recursively.
6. Grant only the traversal/read/execute access required by `codex-worker`.
7. Atomically rename the verified environment into place.
8. Remain idempotent when the verified digest already exists.
9. Retain older digest directories for rollback.

Before tests, the parent dispatcher verifies the marker, ownership, mode, digest, and imports under the same isolated `codex-worker` boundary. It sets a fixed PATH beginning with the selected venv, plus `PYTHONNOUSERSITE=1`, `PYTHONDONTWRITEBYTECODE=1`, `PIP_NO_INDEX=1`, and `PIP_DISABLE_PIP_VERSION_CHECK=1`.

### 4.9 Sanitized reviewer evidence

The parent opens only the expected attempt-local regular `test.log` without following symlinks. It hashes the complete file, then retains at most the final 16 KiB and final 80 logical lines.

Sanitization must:

- decode invalid UTF-8 with replacement;
- normalize line endings;
- remove ANSI/ECMA-48 sequences;
- replace NUL and remaining C0/C1 controls;
- serialize retained lines as ASCII-escaped JSON;
- escape literal `<`, `>`, and `&`;
- record full hash, byte counts, line counts, and truncation;
- detect replacement or mutation during reading.

The prompt places the trusted attestation before the log, labels the log as untrusted evidence, and encloses it in a fixed control-plane delimiter. Fixed review instructions follow the evidence block. Prompt input is provided through stdin or a trusted prompt file, never shell interpolation.

### 4.10 Metrics extraction and definitions

Dispatch 1 extracts the current metrics implementation into `scripts/metrics_report.py` without changing calculations, labels, default selection, ordering, text formatting, JSON fields, exit behavior, or error semantics. `scripts/dispatch.py` retains only a lazy thin alias for the metrics subcommand. The reporting module is not imported during dispatcher startup or any non-metrics command, and no gate code depends on it.

Dispatch 2 changes only the standalone reporting surfaces. `delegation_report.py` may state only whether a mainline commit was matched to retained orchestrator evidence. The complement bucket is named `not_attributed_to_orchestrator`; it must not imply an actor.

The metrics implementation first groups attempt records by spec ID, orders valid attempts numerically, and classifies each spec once:

- **Straight-through:** attempt 1 reached normal `merged`, no replacement/retry/later attempt exists, and retained result evidence shows all normal gates passed.
- **Completed but not straight-through:** terminal failure, replacement/retry, or eventual later-attempt merge.
- **In progress:** no terminal result; excluded from the rate.
- **Malformed/incomplete:** cannot be classified safely; excluded and reported as a diagnostic count.

The output exposes numerator, denominator, excluded counts, and either a numeric rate or `null`. Human-readable output derives from the same structured calculation.

### 4.11 Alternatives considered

1. **Parse `SKIP` from `test.log`.** Rejected because logs are worker-controlled, forgeable, and ambiguous.

2. **Treat exit zero as proof and improve only the reviewer prompt.** Rejected because SPEC-015 would still pass the test gate; reviewer quality cannot repair false control-plane evidence.

3. **Treat every platform skip as a global failure.** Rejected because requiredness depends on changed paths and execution lane. Optional tests may remain recorded as skipped, but a required skip always fails.

4. **Let candidate policy, candidate dispatcher, or candidate test content certify itself.** Rejected because it violates the bootstrap invariant and permits self-exemption or vacuous passing stubs.

5. **Fail every legitimate test-file change permanently.** Rejected because tests must evolve. Parent execution plus supplemental candidate execution, post-install promotion, and the compatibility sequence permit evolution without self-certification.

6. **Accept a candidate replacement test after ordinary reviewer approval.** Rejected because SPEC-015 demonstrates that reviewer prose cannot substitute for mechanically truthful test provenance.

7. **Use schema `minItems` or ask the reviewer to count criteria.** Rejected because a static shared schema does not bind the list to the selected spec. A per-attempt generated schema could encode the count, but dispatcher-side positional validation is simpler to audit and also binds normalized text and indices.

8. **Match criteria as an unordered set or use fuzzy similarity.** Rejected because duplicates and near-matches become ambiguous. Positional normalized equality is deterministic and reviewable.

9. **Search blocker prose for words such as “critical” or “RCE.”** Rejected because substring policies are language-dependent and both miss and invent blockers.

10. **Use several severity levels and choose a threshold.** Rejected for Phase 1 because it creates unnecessary policy decisions. `advisory` versus `blocker` directly models the binary gate.

11. **Expose the operator venv or install per worktree.** Rejected because both weaken isolation and reproducibility.

12. **Provide the complete raw log to the reviewer.** Rejected because it is unbounded and structurally unsafe. The raw artifact remains retained; the prompt receives a bounded representation.

13. **Attribute unmatched merges through Git author/committer identity.** Rejected because author, committer, merger, and automation identity are not equivalent and do not establish the route by which the change entered the branch.

14. **Compute straight-through rate per attempt.** Rejected because retries inflate the population and allow one spec to contribute multiple times.

15. **Let an ordinary-risk metrics worker modify `scripts/dispatch.py` with a function-level scope promise.** Rejected because the scope gate enforces paths, not function boundaries. Extraction is therefore a separate high-assurance predecessor.

16. **Reclassify all metrics semantics as high-assurance without extraction.** Rejected because it leaves a read-only reporting concern unnecessarily coupled to the gate and gives Phase 2 no standalone reporting surface.

17. **Migrate all shell tests in the same dispatch as runner, policy, venv, CI, and reviewer changes.** Rejected because the heterogeneous trust-critical diff would dilute review attention. The homogeneous migration and its mutation proof are separated.

18. **Ship all changes in one high-assurance spec.** Rejected because metrics extraction, metrics semantics, mechanical test migration, repaired attestation, and verdict v4 have distinct risk and activation boundaries.

## 5. Affected boundaries & consumers

| Boundary or consumer | Effect |
|---|---|
| Installed parent dispatcher | Becomes the sole authority for required-test selection, trusted-content execution, attestation validation, test-to-review transition, v4 validation, and blocker evaluation. |
| Installed parent test suite | Supplies authoritative required-test executables, helpers, fixtures, policy bindings, and blob identities. |
| Candidate worktree | Supplies code and data targets plus supplemental test copies only; cannot choose or replace its gate implementation or required trust tests. |
| `scripts/test` users | Receive explicit PASS/SKIP/FAIL summaries and machine attestation rather than exit-code-only aggregation. |
| Existing `tests/*.sh` | Acquire assertion events, exit-77 prerequisite skips, trusted-source/candidate-target separation, and mutation-fidelity validation in a separate dispatch. |
| Candidate test changes | Run additionally, require exact high-assurance approval binding, and become authoritative only after merge, installation, and activation replay. |
| `codex-worker` isolation | Gains read/execute access to one trusted digest venv, never write access or operator-home access. |
| `scripts/verdict.schema.json` | Becomes v4 and rejects legacy/prose-only review output for authorization. |
| Reviewer prompt | Gains trusted attestation and sanitized log evidence; requires structured findings and complete criteria. |
| `evaluate_binary_review()` callers | Malformed findings now fail closed even if schema validation was skipped. |
| Attempt manifests/results | Gain parent-gate identity, trusted-test source bindings, and structured test-attestation references. Historical formats remain display-only. |
| CI job `ci` | Must create a CI-local pinned venv, exercise portable attestation tests, and retain its exact job name and non-matrix form. |
| Setup/operator procedure | Must provision and verify the shared digest venv before activating Dispatch 4. |
| `scripts/metrics_report.py` | Becomes the standalone, read-only metrics implementation and the only ordinary-risk metrics code surface. |
| Dispatcher metrics alias | Remains a lazy compatibility entry point; non-metrics commands do not import the reporting module. |
| Delegation-report consumers | See neutral attribution naming; consumers of renamed JSON properties must update atomically. |
| Dispatch-metrics consumers | Receive spec-level numerator/denominator and nullable rate semantics. |
| Phase 2 measurement work | Remains blocked until Dispatch 2 is merged and validated. |
| Historical SPEC-015 evidence | Read-only regression input; never migrated or rewritten. |
| PR creation | Remains unreachable after failed trusted test attestation, malformed v4, incomplete criteria, or blocker findings. |

Transitive dependency closure includes the dispatcher CLI wrapper, isolated systemd unit construction, attempt result serialization, review-request archive, review-result readers, reporting fixtures, CI, setup verification, parent test installation, target-root adapters, activation drills, and any code that assumes finding fields are strings or that a successful aggregate test exit implies executed trusted tests.

## 6. Ordered implementation steps

1. **Freeze and verify the baseline.** Record the base commit, installed parent executable/commit, requirements digest, current test inventory, current metrics output, metrics alias behavior, and hashes of all SPEC-015/1 evidence. Expand Dispatch 3’s `tests/*.sh` allowance to literal paths before authorization.

2. **Add extraction parity regressions for Dispatch 1.** Capture current metrics text, JSON, exit, selection, and error behavior. Add a non-metrics command probe that detects whether the metrics module is imported.

3. **Extract the metrics implementation.** Move the implementation into `scripts/metrics_report.py`; retain only a lazy thin alias in `scripts/dispatch.py`. Do not change metrics semantics.

4. **Validate and activate Dispatch 1.** Prove parity, prove non-metrics import isolation, run focused tests and `./scripts/test`, complete high-assurance challenge and operator approval, merge to `integration`, and install the extracted parent before ordinary metrics work begins.

5. **Implement Dispatch 2 regression fixtures.** Add `tests/fixtures/metrics_semantics.json` with the exact five-spec population defined in R6. First assert the current rate/attribution behavior fails the new expectations.

6. **Correct attribution naming.** In `scripts/delegation_report.py` and `scripts/metrics_report.py`, rename actor-attributing text and machine fields to `not_attributed_to_orchestrator`; add the actor-unknown explanation and remove compatibility aliases that preserve the false claim.

7. **Correct straight-through calculation.** In `scripts/metrics_report.py`, group by spec, classify each spec once, expose numerator/denominator/exclusions, return `null` for zero denominator, and make text output consume the structured result.

8. **Validate and land Dispatch 2.** Run focused tests and full `./scripts/test`, confirm no trust-closure path changed, dispatch through the installed parent, merge to `integration`, and record that Phase 2’s prerequisite is satisfied.

9. **Add migration-fidelity regressions for Dispatch 3.** Define every assertion-helper form and shell assertion idiom. Add mutation cases that force guarded conditions false and require `ok: false` plus nonzero exit.

10. **Add assertion-event support.** Create `scripts/lib/test_attest.sh`; make helpers evaluate conditions before emitting results and omit any unconditional pass-recording API.

11. **Migrate the tracked shell suite.** Convert assertions and genuine prerequisite skips to assertion events and exit `77`. Add trusted-source/candidate-target root resolution without changing the substantive conditions being tested.

12. **Update the public test wrapper for migration compatibility.** Make `scripts/test` report child exit `77` as `SKIP` while preserving the pre-attestation aggregate behavior needed during the migration predecessor.

13. **Validate and install Dispatch 3.** Run the full suite, the mutation drill, target-root separation probes, challenge, and operator-approved high-assurance dispatch. Merge and install it so its test content and helper become the parent source for Dispatch 4.

14. **Add failing attestation regressions for Dispatch 4.** Cover required skip, zero assertions, missing/duplicate ID, stale nonce, malformed event, inconsistent exit/result, source-blob mismatch, target-root mismatch, policy gaps, candidate-gate marker, candidate required-test stub substitution, reviewer non-invocation, SPEC-015 replay, and adversarial logs.

15. **Introduce the parent runner and policy.** Create `scripts/test_runner.py` and `scripts/test-policy.json`; retain `scripts/test` as the stable entry point. Implement trusted blob resolution, source/target separation, exact ID execution, supplemental candidate-test execution, event validation, nonce/binding checks, atomic aggregate output, and fail-closed status derivation.

16. **Wire attestation into the test gate.** Update `scripts/dispatch.py` to classify actual changed paths, invoke the installed parent runner and tests, validate the aggregate, persist it, and make literal trusted-attestation PASS a precondition for reviewer invocation.

17. **Provision the shared venv.** Extend `scripts/setup-worker-user.sh` with content-addressed atomic provisioning, import/version verification, ownership/mode checks, and idempotence.

18. **Wire the venv into isolation.** Resolve the trusted parent requirements digest, run the worker-boundary import probe, set the fixed environment, expose the venv read-only, and add negative tests for every forbidden fallback.

19. **Add reviewer evidence.** Implement the safe log snapshot/sanitizer and include both trusted attestation and sanitized log tail in the prompt. Preserve the full raw log and hash.

20. **Update CI.** Keep the job named exactly `ci`, do not add a matrix, create a CI-local venv from `scripts/requirements.txt`, and run portable attestation, source-substitution, and negative-skip tests. On-box isolation remains an additional activation requirement.

21. **Replay SPEC-015.** Read the actual recorded log and attempt metadata without writes. Require `legacy_missing_attestation`, no reviewer call, no PR call, and identical before/after hashes.

22. **Authorize, merge, install, and activate Dispatch 4.** Complete high-assurance challenge and dual validation. Verify exact test-delta approval bindings, provision the shared venv, install the merged dispatcher and tests as parent, rerun newly authoritative tests against the exact merged commit, record the parent commit, and pass the on-box isolation drill before starting Dispatch 5.

23. **Add failing verdict-v4 regressions for Dispatch 5.** Cover count mismatch, missing/extra/reordered/duplicate criteria, normalization boundaries, all blocker domains, unknown severities, undeclared properties, malformed evaluator inputs, and legacy display-only behavior.

24. **Implement schema v4.** Update `scripts/verdict.schema.json` with strict top-level versioning, structured criteria, structured finding arrays, required fields, enums, bounds, and `additionalProperties: false` on the v4 top-level object, criterion objects, and finding objects.

25. **Implement dispatcher binding.** Add deterministic normalization, positional count/text/index validation, and explicit failure reasons before calling binary evaluation.

26. **Harden binary evaluation.** Update `evaluate_binary_review()` with the precise blocker predicate and defensive fail-closed behavior. Update the reviewer prompt to request only v4.

27. **Validate and land Dispatch 5.** Run focused and full tests through the newly installed Dispatch 4 parent, complete high-assurance challenge and detached SOL design review, resolve all blockers, and merge only after operator authorization.

## 7. Failure modes & blast radius

| Trigger | Consequence | Mitigation |
|---|---|---|
| Required test skips but exits zero | False test PASS could recur | Assertion count and structured status override aggregate exit; review is never called. |
| Test exits zero without assertions | Empty test body is certified | Zero assertions is an explicit failed-test reason. |
| Candidate removes or renames a required test | Coverage silently disappears | Parent policy and trusted parent IDs still execute; the candidate delta is separately recorded and approved. |
| Candidate rewrites a required test as a passing stub | Candidate-controlled assertions falsely certify coverage | Candidate test results are supplemental only; the verified parent blob supplies the required result. |
| Candidate-added test is treated as authoritative in its own attempt | New gate behavior self-certifies | New tests remain supplemental until merge, installation, and post-install replay from the new parent. |
| Legitimate new behavior contradicts an installed parent test | Safe change cannot pass the old invariant | Use the two-stage compatibility sequence; if impossible, fail closed pending separately scoped bootstrap authority not granted here. |
| Trusted test accidentally targets the parent checkout | Candidate is not actually tested | Explicit source/target roots, target-commit binding, adapter validation, and target-root mismatch failure. |
| Migrated assertion emits PASS before evaluating its condition | Green suite gives vacuous evidence | Helper API ordering plus mutation drills requiring `ok: false` and nonzero exit for induced failures. |
| Candidate edits its policy to exempt itself | Self-certification | Active policy comes from installed parent, not candidate. |
| Candidate gate implementation is invoked | Bootstrap trust collapses | Absolute parent path/commit binding plus marker regression test. |
| Assertion JSON is malformed or replayed | Incorrect attestation | Nonce, sequence, ID, source blob, target commit, base/head, and parent-commit validation; malformed input fails. |
| Shared venv is missing or writable | Tests skip or worker contaminates later gates | Preflight ownership/mode/import checks and no fallback. |
| Requirements change in candidate | Candidate influences gate dependencies | Use parent digest and block pending post-merge provisioning. |
| Reviewer log contains injection text | Reviewer instructions may be displaced | Bounded JSON escaping, fixed delimiters, explicit untrusted-data warning, fixed instructions after evidence. |
| Log changes while read | Reviewer sees evidence not matching retained hash | Stable-file checks; block review. |
| v4 criterion normalization is too permissive | Wrong criterion binds | NFKC plus whitespace only; no case or punctuation folding. |
| v4 criterion normalization is too strict | Harmless formatting rejects verdict | Defined whitespace normalization handles line wrapping; rejection is safe and reviewer may be rerun. |
| Reviewer omits a criterion | Partial PASS | Exact count and positional binding reject it. |
| Reviewer reports a blocker with PASS | Dangerous change advances | Structured blocker predicate forces failure. |
| Reviewer uses an unknown severity | Threshold ambiguity | Unknown is malformed and fail-closed. |
| Evaluator is called without schema validation | Malformed data bypasses controls | Evaluator independently validates finding container, fields, and severity shape. |
| Legacy verdict is supplied to a resumed attempt | Old weak schema authorizes work | Display-only compatibility; authorization requires v4. |
| Ordinary metrics worker can modify dispatcher gate code | Reporting change crosses risk boundary | Behavior-preserving high-assurance extraction precedes semantics; ordinary allowed paths exclude dispatcher files. |
| Metrics module is imported by gate commands | Reporting failure affects authorization availability or behavior | Lazy metrics-only alias and regression proving non-metrics commands do not import the module. |
| Extraction changes metrics behavior before semantic fixtures | Baseline and semantic correction become conflated | Field-for-field parity fixture and separate high-assurance extraction dispatch. |
| Metrics groups attempts incorrectly | Phase 2 learns from distorted rates | Fixed five-spec fixture with invariant expected counts. |
| Neutral merge label breaks a consumer | Reporting job fails | Update all in-repo consumers in Dispatch 2; no misleading alias. Roll back that independent spec if necessary. |
| Zero eligible metrics denominator | Divide-by-zero or deceptive 0% | Return `null`/`N/A` with explicit denominator zero. |
| Migration and gate redesign are reviewed as one heterogeneous diff | Trust-critical mistakes receive diluted review | Separate homogeneous migration from runner, policy, isolation, CI, and prompt changes. |
| New gate is over-strict | Dispatch availability decreases | Fail closed, preserve evidence, repair under a new high-assurance spec; never restore exit-code-only authorization. |
| New gate is under-strict | False PASS and unsafe PR | Worst case is an incorrectly authorized trust-boundary change. Immediately stop dispatch, preserve artifacts, revoke affected authorization, and roll forward the gate before resuming. |

The only irreversible outputs are retained attempt evidence, review artifacts, and any already-opened PR. None may be rewritten. No production deployment or merge to `main` is authorized by this plan.

## 8. Validation plan

### 8.1 Regression proofs that must fail before implementation

1. Run the SPEC-015 replay adapter against its exit-zero log without structured attestation. Current behavior can advance; the new assertion requires a blocking result.
2. Replace a candidate copy of a selected required test with a well-formed passing stub. Candidate-source execution would accept it; the repaired design must still execute and bind the trusted parent blob.
3. Induce a guarded-condition failure after a naïve assertion migration. A miswired migration may remain green; the repaired helper and mutation drill must emit `ok: false` and fail.
4. Validate a PASS containing one `MET` criterion against a multi-criterion spec. Current schema/evaluator accepts the supplied subset; v4 must reject it.
5. Evaluate an all-`MET` PASS whose security finding is a blocker. Current evaluator does not use the content; the new evaluator must reject it.
6. Run the five-spec metrics fixture through current reporting. At least the attribution label or numerator/denominator must disagree with R6.

### 8.2 Dispatch 1 validation

- Capture and compare metrics text output, structured output, ordering, defaults, error output, and exit status before and after extraction.
- Invoke metrics through both the dispatcher alias and standalone reporting module and prove semantic identity.
- Prove representative non-metrics dispatcher commands do not import `scripts/metrics_report.py`.
- Make the reporting module unavailable in a probe and prove non-metrics gate behavior is unaffected.
- Run focused tests and `./scripts/test`.
- Inspect the diff to confirm no dispatcher behavior beyond removal of metrics implementation and creation of the lazy alias changed.
- Require the operator’s per-attempt high-assurance approval artifact.

### 8.3 Dispatch 2 validation

- `tests/delegation_report.sh` proves neutral naming in text and machine output.
- `tests/metrics_report.sh` proves the dispatcher alias obtains its implementation from the standalone module without a Dispatch 2 change to `scripts/dispatch.py`.
- The metrics fixture proves numerator `1`, denominator `3`, rate `1/3`, one in-progress exclusion, and one malformed exclusion.
- A retry-count mutation proves spec-level invariance.
- Empty input proves `null`/`N/A`.
- `./scripts/test` passes.
- The diff contains no dispatcher, gate-transition, authorization, attempt-result, or test-policy path.

### 8.4 Dispatch 3 validation

- `tests/assertion_migration.sh` exercises every assertion-helper form with both a passing and induced-failing condition.
- Every induced failure emits `ok: false` before exit and produces a nonzero exit.
- A representative migrated test for each distinct shell assertion idiom is mutation-tested by forcing the guarded condition false.
- Static inspection confirms no migrated test invokes an unconditional pass-recording primitive.
- Genuine prerequisite absence produces exit `77` and no passing assertion.
- `scripts/test` distinguishes PASS, SKIP, and FAIL while preserving the predecessor’s aggregate compatibility behavior until Dispatch 4 activates.
- Source/target probes prove migrated tests obtain helpers and fixtures from the trusted root while resolving the system under test from the candidate target root.
- The substantive pre-migration and post-migration conditions are mapped in the review artifact; unexplained assertion removal, weakening, or replacement is a blocker.
- Run `./scripts/test`, high-assurance challenge, and operator-approved dispatch before installing the migrated suite as parent content.

### 8.5 Dispatch 4 validation

- `tests/test_attestation.sh` exercises every R1 malformed/negative case and a fully passing multi-test case.
- A required trusted test printing `SKIP` and exiting zero fails at the test gate.
- A required trusted zero-assertion test fails before review.
- A candidate dispatcher marker proves the candidate gate is not invoked.
- A candidate required-test stub that emits correctly formed passing events cannot satisfy or replace the trusted parent result.
- Source-commit, blob-hash, and candidate-target mismatches fail before review.
- Candidate-modified test results are persisted only under `candidate_supplemental_results`.
- `tests/dispatch_gate4.sh` and `tests/dispatch_parallel.sh` execute real trusted assertions against the candidate target with the shared venv rather than skipping.
- `tests/worker_isolation.sh` proves shared-venv read/execute, write denial, home denial, and network denial.
- The real on-box SPEC-015 replay blocks and leaves all historical hashes unchanged.
- The synthetic CI fixture reproduces the same missing-attestation failure.
- Adversarial logs prove the 16 KiB/80-line cap, escaping, control removal, stable hash metadata, delimiter safety, and absence from argv.
- Missing, symlinked, replaced, or raced logs block reviewer invocation.
- `.github/workflows/ci.yml` installs pinned requirements and passes portable tests while retaining job name `ci`.
- `./scripts/test` passes in the provisioned development environment.
- Every test delta in the candidate is bound by exact old/new blob identity in the operator approval artifact.
- After merge, the newly installed parent reruns all newly authoritative tests against the exact merged commit before activation.
- The exact isolated on-box activation drill passes before the new parent is enabled.

### 8.6 Dispatch 5 validation

A table-driven `tests/verdict_integrity.sh` must include:

- N correct criteria: PASS.
- N−1, N+1, duplicate, reordered, wrong-index, invented-text cases: reject.
- Exact and whitespace/NFKC-normalized text: bind.
- Case, punctuation, Markdown, or wording changes: reject.
- Undeclared top-level, criterion, or finding properties: reject.
- All criteria `MET` plus scope blocker: reject.
- All criteria `MET` plus regression blocker: reject.
- All criteria `MET` plus security blocker: reject.
- Empty arrays and advisory-only arrays: eligible to pass.
- Missing, null, string, unknown-severity, or malformed findings: reject.
- Direct calls to `evaluate_binary_review()` without schema preprocessing: malformed inputs reject.
- Wrong spec digest, base SHA, or worker commit: reject.
- v1-v3: displayable but not authorizing.
- Review rejection always yields zero PR-opening calls.

Finally, run `./scripts/test` and the declared per-spec test command through the installed Dispatch 4 parent. High-assurance completion requires both Claude challenge and detached SOL design-review PASS, with all findings resolved and tests rerun after the final change.

## 9. Rollback / irreversibility

1. Roll back in reverse dependency order. Dispatch 5 may be reverted only while dispatch is paused; Dispatch 4 must remain active until no v4-dependent attempt is running.

2. Prefer roll-forward for gate defects. Restoring exit-code-only testing, candidate-content certification, incomplete criterion acceptance, or prose-only blocker handling is not an acceptable availability workaround.

3. If Dispatch 5 must be reverted, preserve every v4 artifact and add or retain display-only readers. Do not reinterpret v4 artifacts through the v3 authorization path.

4. If Dispatch 4 must be reverted, stop all new dispatches first, let active isolated units terminate, and revert dispatcher, runner, policy, CI, setup, and prompt changes as one internally consistent unit. Keep the independently installed assertion migration unless it is itself defective. Do not convert new failed-test outcomes to legacy passes.

5. If Dispatch 3 must be reverted, pause dispatch, restore `scripts/test`, the helper, and all migrated tests as one unit, and rerun substantive pre-migration tests. Do not leave a mixed suite in which some tests emit attestations and others silently rely on exit zero.

6. Digest-addressed venvs may remain in place; they are inert if unreferenced and support forward recovery. Remove one only after proving no active unit references it.

7. Dispatch 2 is independently reversible if a reporting consumer breaks. Its fixture and corrected definitions must remain the target semantics for the replacement; rollback does not unblock Phase 2.

8. Dispatch 1 may be reverted only through a high-assurance dispatcher change. If reverted, Dispatch 2’s standalone semantics must not be copied back into an ordinary-risk `scripts/dispatch.py` change.

9. Existing attempts, including SPEC-015, are irreversible evidence. Never edit or delete them during rollback.

10. After any rollback, rerun the applicable pre-change suite, integrity hashes, worker-isolation drill, trusted-test source/target drill, and parent-gate marker test before dispatch resumes.

## 10. Open questions / operator decisions

None. The operator’s existing per-dispatch approval, merge, installation, shared-venv provisioning, and activation duties remain required operational steps, not unresolved design decisions. An incompatible bootstrap outside the §4.4 compatibility sequence is explicitly not authorized by this plan and would require a new operator decision and artifact.

## 11. Provenance (filled during challenge/authorization — NOT by the drafter)

- **Challenge (Claude):** REVISE, with objections O1-O5 dated 2026-07-14. O1, O2, and O5 were blocking.
- **Disposition (drafter):** Revision 2 applies all five objections as recorded below.
- **Dual-validation (high-assurance/control-plane):** Fresh-context SOL adversarial critique of this same revision: pending. Claude verdict on this revision: pending. Unresolved disagreement escalates to operator.
- **Authorization:** Pending authorized-revision digest, authorizer, and date. Every high-assurance dispatch requires its per-attempt approval artifact. Any silent post-authorization edit voids authorization.
- **Completion reconciliation:** Pending; record followed / authorized-deviation / unauthorized-deviation after all five dispatches.

## Disposition record (revision 2)

- **O1 — revise.** Corrected the sole frontmatter identity and body title to `PLAN-003`, removed the duplicated pseudo-frontmatter, and retained applicable metadata in the real YAML frontmatter. Sections changed: YAML frontmatter; document title; §11.

- **O2 — accept-and-amend.** Adopted trusted parent/base content execution rather than allowing candidate copies to satisfy required tests. Required tests now resolve to verified blobs from the installed parent commit and target the candidate through a distinct target-root interface. Candidate-modified copies run only as separately namespaced supplemental tests. Legitimate test changes use post-install promotion; changes contradicting the old test use a two-stage compatibility sequence, while impossible compatibility cases fail closed pending separately scoped bootstrap authority not granted here. Added source/blob/target bindings, stub-substitution acceptance criteria, candidate-added-test handling, failure modes, approval bindings, and activation validation. Sections changed: §1; §2.2; R1; R5; R7; §§4.2-4.5; §5; implementation steps 14-22 in §6; §7; §§8.1 and 8.5; §9; §10.

- **O3 — accept-and-amend.** Added a required migration-fidelity mutation drill covering every assertion-helper form and a representative migrated test for each distinct shell assertion idiom. An induced guarded-condition failure must emit `ok: false` and exit nonzero, and the helper exposes no unconditional pass-recording API. Sections changed: §2.2 assumption 4; R1 acceptance criterion 10; §§4.5 and 4.11; Dispatch 3 paths in §4.2; implementation steps 9-13 in §6; §7; §§8.1 and 8.4.

- **O4 — accept-and-amend.** Split the homogeneous assertion-event, exit-77, and target-root migration into its own high-assurance predecessor. Runner, policy, dispatcher gate logic, venv, CI, and reviewer-evidence work remain in a following high-assurance dispatch. The verdict-v4 work is now the fifth dispatch and runs under the installed repaired parent. Sections changed: §1; §§4.1-4.2; §5; §6; §7; §§8.4-8.6; §9; §11.

- **O5 — accept-and-amend.** Adopted preferred option (a) with a necessary two-dispatch boundary: a high-assurance behavior-preserving extraction first moves metrics into `scripts/metrics_report.py` and leaves a lazy thin alias in `scripts/dispatch.py`; the following ordinary-risk semantic correction excludes both dispatcher files from its allowed paths. Added parity, lazy-import isolation, trust-closure acceptance criteria, failure modes, and rollback rules. Sections changed: §1; §2.1 fact 13; R6; §§4.1-4.2; §4.10; §4.11 alternatives 15-16; §5; implementation steps 2-8 in §6; §7; §§8.2-8.3; §9; §11.


=== SOL FRESH-CONTEXT CRITIQUE (VERDICT: BLOCK) ===
PLAN-003 is not safe to authorize. It closes the exact “exit zero despite SKIP” symptom, but it does not establish that the trusted assertions—not candidate code—produced the attestation, that all required assertions ran, or that the tested candidate tree is the tree later reviewed. Worse, Dispatches 1–3 and Dispatch 4’s activation occur before a non-self-gating verifier exists.

The current failure is real: the gate trusts the child return code ([dispatch.py](/home/val/orchestrator/scripts/dispatch.py:935)), the wrapper maps zero to PASS ([scripts/test](/home/val/orchestrator/scripts/test:22)), and SPEC-015 contains successful prerequisite skips ([test.log](/home/val/orchestrator/.orchestrator/attempts/SPEC-015/1/test.log:7), [test.log](/home/val/orchestrator/.orchestrator/attempts/SPEC-015/1/test.log:10), [test.log](/home/val/orchestrator/.orchestrator/attempts/SPEC-015/1/test.log:30)). PLAN-003 still permits related vacuity.

## Blocking findings

### B1. The five-dispatch sequence bootstraps through the gate known to be unsound

Dispatches 1, 2, and 3 run before Dispatch 4 introduces attestation. Dispatch 3 changes every test and the assertion helper while the installed gate still trusts aggregate exit status. Dispatch 4 then introduces its own runner, policy, helper interpretation, and activation tests as candidate content. Its post-merge “new parent tests itself” drill is still self-certification: the newly introduced policy and runner decide what their own activation must execute.

The current parent cannot enforce R1 or distinguish a candidate’s substantive tests from a vacuous `test_command`; it only sees the return code at [dispatch.py](/home/val/orchestrator/scripts/dispatch.py:935). Exact blob lists and human high-assurance review do not satisfy the stated “no candidate self-gating” invariant.

Minimal amendment: introduce a separately authorized bootstrap gate before Dispatch 1. It must be installed outside the candidate commit, contain a frozen required-test inventory and expected assertion inventory, execute candidate code through the worker boundary, and gate all five dispatches. Dispatch 4’s runner and policy must be validated by that prior bootstrap gate, not activated based only on their own tests. Alternatively, reorder the work so a minimal independently installed attestation gate is the first change.

### B2. “At least one assertion” still certifies vacuous or partially unrun tests

R1 requires only a positive assertion count. A trusted test can execute one trivial setup assertion, bypass every substantive branch, and exit zero. It then satisfies the planned aggregate even though the important test body never ran. The migration drill covers one representative per shell idiom, not every migrated assertion or every required branch.

This is the same failure class in a more structured format: “some assertion ran” replaces “some process exited zero.”

The retained free-form `test_command` is worse. R1 acceptance criterion 5 explicitly allows `test_command: true`; §4.5 requires only that this supplemental command exit zero. Thus the gate may report successful testing even when the spec-declared test executed no assertion at all.

Minimal amendment:

- Give each required test a parent-owned exact set of required assertion IDs, not merely a minimum count.
- Require every expected assertion exactly once, with no unknown or duplicate assertion.
- Mutation-test every migrated assertion or explicitly justified conditional assertion, not one representative per idiom.
- Remove free-form `test_command` from authorization evidence, or subject it to an attested parent-owned test-ID/expected-assertion contract. A `true` command must never be represented as successful test evidence.

### B3. The nonce labels events but does not authenticate their producer

The plan injects the nonce, test ID, trusted root, target root, and assertion channel into the trusted test environment. Candidate code invoked by that test normally inherits environment variables and open descriptors. A malicious candidate implementation—not merely a candidate copy of the test—can therefore emit valid events for the trusted test.

Acceptance criterion R1.8 tests the wrong adversary: a candidate test stub. It does not test candidate system-under-test code reading the nonce or writing the trusted harness’s event channel. Separate aggregate namespaces do not help if both executions can reach the same live channel or its credentials.

Nonce secrecy is especially weak when harness and candidate run as the same `codex-worker` UID. Depending on `/proc` and ptrace settings, candidate code may also inspect ancestor environments or duplicate descriptors.

Minimal amendment: specify a real assertion-producer boundary. At minimum:

- Candidate processes receive neither nonce nor assertion descriptor.
- The descriptor is closed before every candidate `exec`.
- Assertion helpers scrub all attestation variables when invoking candidate commands.
- Trusted and supplemental executions use separate pipes and per-invocation nonces.
- The trusted controller alone converts observed command outcomes into assertion events.
- Add an end-to-end attack where candidate application code tries environment, inherited-FD, `/proc/<ppid>/environ`, and `/proc/<ppid>/fd` forgery.

If same-UID isolation cannot make that robust, the trusted controller and candidate process need distinct security principals.

### B4. The tested target is not bound immutably to `worker_commit`

Recording `candidate_target_root` and `worker_commit` does not prove the worktree bytes equal that commit. Candidate code can modify its worktree during a trusted test. The supplemental `test_command` can mutate it after required tests finish. The reviewer can then receive a different diff/tree from the one tested.

The current gate already runs a child command and retains only its result ([dispatch.py](/home/val/orchestrator/scripts/dispatch.py:935)); PLAN-003 adds no explicit read-only candidate snapshot, post-command clean-tree verification, or tree digest revalidation before review and PR creation.

Minimal amendment: test an immutable candidate snapshot. Use a read-only bind of the exact committed tree plus a separate writable build/output directory. Revalidate the commit, index, tracked bytes, untracked policy, and tree identity after every candidate-controlled process and immediately before review and PR opening. Add a candidate program and supplemental test that mutate tracked and untracked inputs after emitting PASS.

### B5. §4.4 does not define a race-safe blob execution mechanism

“Execute the verified descriptor or an equivalently race-safe path” leaves the critical operation to implementer discretion. For shell scripts, verifying an open descriptor and then executing a pathname is a TOCTOU. Executing `/proc/self/fd/N` has interpreter and descriptor-lifetime complications. Directory components, sourced helpers, fixtures, policy, interpreters, and the runner itself also belong to the trust closure.

The plan records a blob hash per test but does not require a complete closure manifest or race tests. A parent checkout being associated with a commit is not proof that its working-tree files still match that commit.

Minimal amendment:

- Select one concrete mechanism: for example, a root-owned, read-only installed snapshot materialized from the exact commit, with a closure manifest for runner, policy, tests, helpers, fixtures, schema, and wrapper.
- Define component-by-component `openat2`/`O_NOFOLLOW` rules or immutable copied-blob execution.
- Never reopen a verified pathname for execution.
- Verify interpreter identity and helper/fixture blob identities.
- Add concurrent replacement, directory-symlink swap, helper swap, fixture swap, and interpreter-shadow tests.

The plan also needs a worker-readable trusted install root outside the operator’s home. The repository convention says the operator’s home is unreachable to `codex-worker`, while §4.4 assumes tests execute from the installed parent checkout. A root-owned `/srv/codexwork/installed/<commit>` snapshot or equivalent is required.

### B6. New evidence is not made immutable

Temporary-file plus `fsync` plus atomic rename provides crash consistency, not append-only evidence. A normal atomic replace can overwrite an existing result. The plan does not require `O_EXCL`/no-replace semantics, directory `fsync`, trusted ownership of the attempt directory, single-link files, or refusal to reopen a finalized attempt for writing.

R7.4 tests only that old SPEC-015 bytes remain unchanged. That does not validate immutability of new logs, aggregates, review requests, results, or resumed attempts.

Minimal amendment:

- The dispatcher, not a worker-UID runner, owns the attempt directory and final evidence files.
- Worker output crosses a pipe; candidate code never gets a writable evidence pathname.
- Create each artifact once with no-replace semantics; reject an existing destination.
- Verify regular file, owner, mode, link count, and directory ownership.
- `fsync` files and containing directories.
- Seal a terminal manifest containing hashes of all artifacts.
- Add overwrite, hardlink, symlink, rename-swap, resume-after-terminal, and concurrent-writer tests.

### B7. Supplemental namespaces are logical labels, not demonstrated isolation

The plan says supplemental events cannot overwrite or satisfy trusted results, but does not specify distinct channels, credentials, file destinations, or OS permissions. If trusted and supplemental processes share the runner UID, nonce environment, event pipe, or result directory, a malicious supplemental process can race, duplicate, or precreate results.

Likewise, “one result in the aggregate” does not prove a test was launched exactly once; a runner could launch twice and retain one result.

Minimal amendment: separate invocation pipes and nonces, strict expected source class, dispatcher-owned aggregation, and no worker-writable aggregate path. Add a side-effect counter proving exact single launch under parallel execution, plus attacks that emit trusted IDs from supplemental and candidate-application processes.

### B8. Reviewer-log “sanitization” cannot neutralize semantic prompt injection

JSON escaping and delimiter escaping prevent structural delimiter closure. They do not prevent a model from following instruction-like text encoded inside the JSON string. Therefore R5.3’s claim that instruction-like text “cannot alter fixed reviewer instructions” is not falsifiable by string-level tests.

The reviewer already receives candidate-controlled diff text at [dispatch.py](/home/val/orchestrator/scripts/dispatch.py:1141) and [dispatch.py](/home/val/orchestrator/scripts/dispatch.py:1188). A sanitized log adds another semantic injection channel. Fixed instructions after the evidence reduce risk but do not establish non-interference.

Minimal amendment: narrow the acceptance claim to syntactic prompt integrity. Do not claim semantic neutralization. Prefer machine-generated attestation summaries over arbitrary log text; if the tail remains, classify the reviewer as an untrusted advisory oracle and require deterministic gates for all mechanically decidable scope/test properties. Adversarial model trials may measure risk but cannot prove the invariant.

Also add a raw-log size/resource limit. Hashing an unlimited log after the process fills disk is not a safe “oversized evidence” policy.

### B9. The venv identity and poisoning model are incomplete

The environment path is addressed only by the requirements-file hash. Two boxes with different Python versions, pip/resolver versions, platform wheels, or transitive artifacts can produce different environments at the same path. The marker records Python version but the address does not; an upgrade creates a collision that the stated idempotent installer cannot resolve cleanly.

Read-only permissions prevent direct writes but not import shadowing. Running Python with the candidate directory as cwd can load candidate `yaml.py`, `jsonschema.py`, `sitecustomize.py`, or other shadow modules before the venv package. A fixed PATH and `PYTHONNOUSERSITE` do not close this.

Minimal amendment:

- Address the environment by requirements lock hash, Python ABI/platform, and installer/wheel-lock identity.
- Use hash-locked artifacts or an approved wheelhouse; verify transitive package hashes.
- Invoke trusted Python with isolated import semantics, safe cwd, scrubbed `PYTHONPATH`/Python variables, and verified module origins under the venv.
- Verify recursive ownership, ACLs, symlinks, and writability, not just top-level modes.
- Add malicious target-root shadow modules and `sitecustomize` tests.
- Add a fresh-box provisioning test and a Python-version-change/idempotence test.

The candidate-requirements rule is also contradictory: the candidate requirements are not trusted during its gate, but provisioning after merge risks making those requirements part of the environment used to validate their own activation. Define a staged dependency-upgrade bootstrap.

### B10. Required-test policy coverage is not exhaustively testable as written

The policy language is sensible, but §8 tests only selected gaps. Missing cases include mode-only changes, symlink/type changes, submodules, `.gitmodules`, delete/add rename evasion, root-level imported modules, generated configuration, untracked files, and dirty-worktree state.

Minimal amendment: define the canonical Git diff algorithm and file-state model. Property-test every changed path to produce either a nonempty parent-required set or a policy error. Include old/new rename paths, modes, object types, submodules, unknown root paths, and worktree dirt. Bind policy evaluation to the exact base and candidate trees, not a mutable checkout.

### B11. Approval requirements contradict the declared scope

R7.6 requires approvals bound to the exact dispatch, attempt, and test-content digest. The repository convention stores approvals at `.orchestrator/approvals/<digest>.json`; PLAN-003 simultaneously declares approval-binding findings a non-goal. Dispatch 4 also requires approval artifacts to enumerate old/new test blobs, but no approval schema/validator change is assigned an allowed path.

This requirement is therefore not implementable or validatable under the stated scope.

Minimal amendment: either add approval artifact schema, validation, and tests as a separately bootstrapped high-assurance dispatch, or remove attempt/test-blob binding from R7 and explicitly accept the weaker existing invariant. The current contradiction must not remain.

### B12. Verdict v4 binds shape and position, but parsing and spec provenance remain underdefined

Current validation binds only the three artifact identities ([dispatch.py](/home/val/orchestrator/scripts/dispatch.py:1119)), while the evaluator accepts the supplied criterion subset ([dispatch.py](/home/val/orchestrator/scripts/dispatch.py:1106)); v4 correctly addresses that gap. But two authorization ambiguities remain:

- Standard JSON parsing commonly accepts duplicate object keys, retaining the last. A file containing two `severity`, `verdict`, or binding keys can be interpreted differently by humans and machines.
- “The bound spec’s acceptance criteria” is not tied to an immutable opened spec snapshot. Rehashing a path and later reopening it leaves a spec TOCTOU similar to the test-blob issue.

Minimal amendment: reject duplicate JSON keys and nonstandard constants during parsing. Persist the exact digest-approved criteria vector in the attempt manifest before candidate work and validate against that immutable vector. Add duplicate-key and concurrent spec-replacement tests.

Legacy display compatibility also needs a complete consumer inventory. Replacing the shared schema while asserting separate display-only readers exist can go green in the new authorization tests while breaking an unenumerated historical reader.

## Operational and sequencing defects

- Phase 2 is allowed after Dispatch 2 even though Phase 1’s integrity repair is not complete. If corrected metrics are merely one prerequisite, state that; otherwise Phase 2 must wait for Dispatch 5 and its activated parent.
- Installation and activation are checklist prose, not a machine-enforced state transition. There is no defined activation manifest, command, storage location, or refusal rule when the on-box drill is missing.
- CI can remain green while on-box tests are skipped. The current workflow’s aggregate success cannot establish worker isolation; that is already demonstrated by SPEC-015’s skipped isolation test. CI should emit only a portable validation artifact, while an on-box activation artifact must be mechanically required before the parent can dispatch.
- The two-stage compatibility procedure is not generally usable. A transitional test that accepts old and new behavior becomes a weak authoritative parent test; a strict new test then needs another promotion cycle. Define the actual three-state sequence, or require the external bootstrap authorization immediately.
- A five-merge chain needs exact installed-commit checks and exclusion of intervening integration changes. “Merged and installed” must be a recorded, verified state, not an operator recollection.
- A fresh box needs a tested provisioning path for the worker UID, trusted install snapshot, locked venv, systemd properties, ACLs, and activation manifest. CI-local venv creation does not exercise that path.

## Assumption 4 is not demonstrated

A representative-per-idiom mutation drill cannot prove a suite-wide migration is mechanical. Shell semantics vary with `set -e`, pipelines, subshells, command substitution, traps, quoting, redirection, and status capture. One correctly migrated `grep` assertion says nothing about another occurrence whose status is accidentally masked.

The minimal credible migration proof is a parent-owned assertion inventory per test, exact assertion IDs, and an induced failure for every migrated substantive assertion or a specific, reviewed justification for why it cannot be independently mutated. Pre/post mapping prose alone is not a falsifiable gate.

## Acceptance tests missing from §8

These tests should fail against today’s implementation and pass only after the corresponding invariant exists:

1. A pre-Dispatch-4 candidate replaces the test wrapper/helper with an exit-zero stub; the bootstrap gate must still reject it.
2. Candidate application code forges trusted events using inherited environment, descriptors, and `/proc`.
3. A required test emits one trivial assertion while bypassing all expected substantive assertions.
4. The spec’s supplemental `test_command` is `true` or emits no assertions.
5. Candidate code changes the tested worktree after PASS and before review.
6. A parent test/helper/fixture is replaced between verification and execution.
7. The installed parent runner, policy, schema, or helper drifts from its recorded commit.
8. Trusted and supplemental executions race with the same test ID.
9. A test is launched twice but only one result is persisted.
10. An existing finalized aggregate/log/result is overwritten or replaced through symlink/hardlink/rename.
11. Activation is attempted without the prior-parent/bootstrap validation artifact.
12. CI passes while the required on-box activation artifact is absent; activation must still fail.
13. A candidate target shadows `yaml`, `jsonschema`, or `sitecustomize`.
14. Fresh-box provisioning and Python-version changes produce the correct distinct venv identity.
15. Verdict JSON contains duplicate top-level and nested keys.
16. The bound spec file is replaced between digest validation and criterion extraction.
17. Policy classification covers mode changes, symlinks, submodules, unknown root paths, untracked files, and dirty target state.
18. The full two-/three-stage compatibility sequence is exercised end to end.

## Validation paths that can falsely go green

- The SPEC-015 adapter can reject every legacy log without exercising the production dispatcher transition. The replay must enter through the real gate state machine and prove reviewer/PR spies remain untouched.
- A sanitizer unit test can prove escaped bytes while saying nothing about reviewer semantic prompt injection.
- A constructed aggregate can pass validator tests without proving the real isolated process produced it.
- Candidate-copy substitution tests do not cover candidate application-code forgery.
- `./scripts/test` can be green while lane-required on-box tests did not authorize activation.
- The post-install drill can pass because the new policy and tests selected themselves.
- Candidate-authored extraction parity fixtures can bless changed output. Baseline output hashes must be captured before candidate work and approval-bound.
- The five-spec metrics fixture does not define window-boundary behavior. A retry before the selected window or later attempt after it can be misclassified. Eligibility should be anchored—normally by terminal-spec time—and classification must consult the complete attempt history.
- Hashing SPEC-015 before and after proves that one historical directory was untouched; it does not prove append-only behavior for new attempts.

## Overall assessment

Parent-blob verification, positional criterion binding, and structured blocker severities are good components. They authenticate inputs and eliminate several ambiguous representations. They do not, as designed, authenticate the live assertion producer, require the complete assertion set, freeze the candidate target, or provide a non-self-gating bootstrap. Those are control-plane defects, not implementation details.

Repository-access note: local shell execution was blocked before command execution by the environment’s `bwrap: loopback: Failed RTM_NEWADDR` error. The repository citations above are therefore the exact live path/line locations supplied in PLAN-003’s evidence section; the blocking design defects follow independently from the proposed protocol and sequencing.

VERDICT: BLOCK


=== CLAUDE DISPOSITION TABLE ===
# Claude disposition — SOL critique round 1 of PLAN-003 rev2 (VERDICT: BLOCK)

Tier: Critical (gate/evidence integrity) → iterate on the same revision until Claude + SOL both
report no unresolved blocker. My per-finding disposition below; rev3 must address every SUSTAINED
item. Where I scale an amendment down from SOL's maximal form, the invariant is kept and the
residual is recorded explicitly in the plan — hiding it voids the disposition.

| Finding | Disposition | Notes |
|---|---|---|
| B1 bootstrap self-gating | SUSTAIN, scaled | Add a minimal operator-installed bootstrap verifier BEFORE Dispatch 1: frozen (non-candidate) script + frozen required-test/assertion inventory, run by the operator/Claude on-box against each of Dispatches 1–4; Dispatch 4's activation is validated by this frozen verifier, never by its own tests. A full second gate system is out of scope — record residual. |
| B2 assertion-count vacuity | SUSTAIN | Parent-owned expected-assertion-ID set per required test, exact-set match (no unknown/duplicate/missing). Mutation drill: every substantive assertion in TRUST-class tests, or a per-assertion written justification; representative-per-idiom stays only for non-trust tests. `test_command` output is never test evidence unless attested; `true` never counts as testing. |
| B3 nonce/producer authentication | SUSTAIN | Candidate processes get neither nonce nor event descriptor: helpers scrub attestation env + close FDs before any candidate exec; per-invocation nonces + separate pipes (trusted vs supplemental); trusted controller alone converts outcomes to events. Add the candidate-application-forgery attack tests (env, inherited FD, /proc/ppid). |
| B4 target not frozen | SUSTAIN, scaled | Mandatory tree revalidation: after the test phase and immediately before review AND before PR creation, re-verify worktree == worker_commit (clean status, tree hash match); mismatch = failed_test/failed_review. Read-only bind mount is optional hardening, not required for rev3. |
| B5 race-safe execution + trusted root | SUSTAIN | Concrete mechanism required: root-owned read-only snapshot at /srv/codexwork/installed/<commit> materialized from the exact parent commit with a closure manifest (runner, policy, tests, helpers, fixtures, schema, interpreter identity); no reopen-by-path after verification; add swap/race tests. The operator-home-unreadable point is confirmed correct on this host (D5 DAC boundary). |
| B6 evidence immutability | PARTIAL | Full evidence hashing/sealing is the separate `evidence-hashing` plan (baton action 3) — do NOT duplicate it here. PLAN-003 rev3 adds only: O_EXCL no-replace creation for all NEW attestation/review artifacts + dispatcher-owned (not worker-writable) evidence paths + refusal to reopen a finalized attempt. Cross-reference the evidence-hashing plan for the rest. |
| B7 supplemental namespace isolation | SUSTAIN | Fold into B3 mechanics: separate pipes/nonces, dispatcher-owned aggregation, single-launch proof (side-effect counter). |
| B8 semantic injection claim | SUSTAIN (claim narrowing) | R5.3 claims syntactic prompt integrity ONLY; reviewer stays an untrusted fail-closed oracle layered over deterministic gates; prefer machine-generated attestation summary as primary evidence, log tail secondary. Add raw-log size cap BEFORE hashing (cap enforced at capture). |
| B9 venv identity/shadowing | SUSTAIN, scaled | python -I (isolated), fixed safe cwd, scrubbed PYTHON* env, venv address = hash(requirements + python ABI/platform); verify module origins for yaml/jsonschema resolve under the venv; add candidate shadow-module (yaml.py/sitecustomize.py) tests + fresh-box and python-upgrade tests. Full hash-locked wheelhouse: record as residual/deferred unless requirements.txt already pins hashes. Candidate requirements-change bootstrap: staged (provision from MERGED parent only, never pre-merge). |
| B10 policy coverage | SUSTAIN, scaled | Define the canonical diff model (mode changes, symlinks, submodules, type changes, renames both sides, untracked/dirty = fail closed in control-plane areas); property-test: every changed path yields nonempty required set or policy error. |
| B11 approval-binding contradiction | SUSTAIN (routing) | The approval-schema work is its own plan draft (baton action 4). rev3 must pick: (a) declare dependency on that plan landing first and bind test-blob enumeration through its schema, or (b) drop R7.6's new binding claims to the existing invariant and record the gap explicitly. No silent contradiction. |
| B12 duplicate keys + spec snapshot | SUSTAIN | Reject duplicate JSON keys at parse (object_pairs_hook); persist the digest-approved acceptance-criteria vector in the attempt manifest at launch; validate v4 against that immutable vector; add duplicate-key + spec-swap tests. |
| Operational: activation manifest | SUSTAIN | "Merged and installed" becomes a recorded machine-checked state: an activation manifest (commit, drill results, verifier hash) that the dispatcher requires before dispatching under a new parent; missing manifest = refuse. |
| Operational: Phase 2 gating | SUSTAIN (clarify) | Phase 2 implementation dispatch requires ALL of Phase 1 landed (through Dispatch 5), matching the operator's gate; metrics (Dispatch 2) alone is necessary but not sufficient. |
| Operational: compatibility sequence | SUSTAIN | Specify the full three-state sequence explicitly (transitional test → promote strict test → remove bridge), each stage a normal dispatch. |
| Operational: fresh-box provisioning | SUSTAIN, scaled | One documented + tested provisioning script path (extends setup-worker-user.sh); CI never claims to cover it. |
| Assumption 4 | SUSTAIN | Superseded by B2's per-assertion inventory for trust tests. |
| §8 additions / falsely-green list | SUSTAIN | Fold SOL's 18 missing acceptance tests and the falsely-green mitigations (replay through the REAL gate state machine with reviewer/PR spies; baseline parity hashes captured pre-candidate and approval-bound; metrics window anchoring) into R-numbered acceptance criteria. |

Net effect on decomposition: rev3 should add a **Dispatch 0 (bootstrap verifier + activation
manifest, operator-installed, high-assurance)** and may fold B5's install-snapshot provisioning
into it; Dispatches renumber accordingly. Residual-risk section must list: no full wheelhouse
hash-locking (if deferred), read-only bind mount optional, semantic injection not neutralizable,
full evidence sealing in the separate evidence-hashing plan.
warning: Codex's Linux sandbox uses bubblewrap and needs access to create user namespaces.
codex
I’ll rebuild the plan as revision 3, preserving rev2’s sound pieces while applying Claude’s scaled dispositions exactly. I’ll first verify the live repository citations and template, then draft the complete six-dispatch plan with numbered, mechanically testable criteria and the required residual/disposition records.
codex
The shell is blocked before command execution by the same sandbox networking failure noted in the critique (`bwrap: loopback: Failed RTM_NEWADDR`). I’m checking for a read-only workspace resource path so I can still verify citations; if none is available, I’ll retain only citations already corroborated by both rev2 and the fresh-context review and explicitly mark that limitation.
mcp: codex/list_mcp_resource_templates started
mcp: codex/list_mcp_resources started
mcp: codex/list_mcp_resource_templates (completed)
mcp: codex/list_mcp_resources (completed)
codex
The revision’s core structure is now settled: option (a) for B11 (the approval-schema plan must land first), then Dispatch 0 as a frozen operator-installed verifier/activation layer, followed by the five existing dispatches. I’m preserving the scaled boundaries: no mandatory read-only bind mount, no full second gate, no duplicated evidence-sealing design, and no claim that log escaping neutralizes semantic prompt injection.
codex
---
id: PLAN-003
created: 2026-07-14T05:42:58Z
author_model: gpt-5.6-sol
status: challenged
task: "PHASE-1 PLAN (verdict integrity + metrics semantics) — redraft to the PLAN-TEMPLATE.md standard"
ledger_ref: R19
lane: mixed
supersedes: drafts/verdict-integrity/PLAN-001
revision: 3
---

# PLAN-003 — Phase 1 verdict integrity and metrics semantics

## 1. Decision & non-goals

Phase 1 will establish a non-self-gating path before changing the existing gate. A new Dispatch 0 will install a minimal frozen bootstrap verifier outside candidate-controlled repository content. That verifier will carry a frozen required-test inventory and a frozen expected-assertion-ID inventory, run candidate code through the worker boundary, provision root-owned trusted snapshots under `/srv/codexwork/installed/<commit>`, and issue machine-checked activation manifests. It will gate Dispatches 1–4 and will validate Dispatch 4’s activation; Dispatch 4 may not activate by selecting and passing only its own new policy or tests.

After Dispatch 0:

- Dispatch 1 performs a high-assurance, behavior-preserving metrics extraction.
- Dispatch 2 performs the ordinary-risk metrics semantic correction.
- Dispatch 3 performs the high-assurance assertion migration.
- Dispatch 4 installs the repaired parent-owned test-attestation gate, isolated environment, source/target binding, tree revalidation, activation enforcement, and reviewer evidence.
- Dispatch 5 installs verdict schema v4, positional criterion binding, duplicate-key rejection, and structured blocking findings under the activated Dispatch 4 parent.

The installed parent dispatcher—not candidate content—will select required tests from the exact approved base and candidate Git trees. Each required test will have a parent-owned exact expected-assertion-ID set. Authorization requires an exact set match: every expected ID exactly once, with no missing, unknown, or duplicate assertion.

Trusted assertion events will be producer-authenticated operationally. Candidate application processes receive neither an attestation nonce nor an assertion descriptor. Parent-owned command helpers scrub attestation and Python environment variables, close all non-whitelisted descriptors before every candidate `exec`, and use per-invocation nonces and separate trusted and supplemental pipes. The trusted controller alone converts observed command outcomes into assertion events and owns aggregation.

Required test content, runner, policy, helpers, fixtures, schemas, wrappers, and interpreter identities will come from a root-owned, read-only installed snapshot materialized from the exact parent commit. Candidate-modified tests remain supplemental. The candidate tree will be revalidated after the test phase, immediately before review, and immediately before PR creation.

Review verdict schema v4 will retain positional binding: one ordered entry for every acceptance criterion persisted in the attempt manifest before candidate work. Review JSON parsing will reject duplicate object keys and nonstandard constants. Structured scope, regression, and security blockers will mechanically prevent PASS.

Metrics will retain the extraction/semantics split. Extraction parity baselines will be captured by the operator before candidate work and bound by the approval artifact. Straight-through metrics will be anchored by terminal-spec time while consulting the spec’s complete attempt history.

Phase 2 implementation work requires all six Phase 1 dispatches—Dispatches 0–5—to be completed, merged where applicable, installed, activated, and validated. Dispatch 2 is necessary but not sufficient.

This plan chooses B11 option **(a)**: the separate approval-schema plan from baton action 4 must land and activate before Dispatch 0. PLAN-003 will use that schema to bind exact dispatch, attempt, verifier, baseline, inventory, and changed-test blob identities. Weakening the approval invariant would leave the highest-risk bootstrap and test-migration changes without machine-validatable authorization.

Non-goals:

- Do not create a complete second production gate in Dispatch 0. It is a deliberately minimal, frozen bootstrap verifier.
- Do not require a read-only bind mount of the candidate tree in revision 3. Tree revalidation is mandatory; a read-only bind is optional hardening.
- Do not duplicate the full sealing, terminal-manifest, and evidence-hash-chain design assigned to the separate `evidence-hashing` plan.
- Do not claim that escaping or delimiting reviewer log text neutralizes semantic prompt injection.
- Do not add a hash-locked wheelhouse or redesign dependency locking in this plan.
- Do not change the worker model, reasoning depth, priority service tier, reviewer seat, branch topology, or human merge authority.
- Do not permit candidate dispatcher, runner, policy, schema, helper, fixture, or test content to authorize itself.
- Do not treat aggregate exit zero, a positive assertion count, free-form `test_command` output, or reviewer prose as proof that required tests ran.
- Do not expose the operator’s home or `.venv`, install packages as `codex-worker`, enable candidate network access, or import Python modules from the candidate root into the trusted controller.
- Do not alter `.orchestrator/attempts/SPEC-015/1/*`.
- Do not retroactively authorize legacy verdicts or test logs.
- Do not infer an unmatched merge’s actor.
- Do not authorize an incompatible test change outside the three-state compatibility sequence or a separately approved external bootstrap.
- Do not allow CI’s portable suite to substitute for an on-box activation artifact.

## 2. Current-state evidence

### 2.1 Observed facts

1. The current test gate records `test.log` and trusts the child return code; it does not prove which required tests or assertions executed ([scripts/dispatch.py](/home/val/orchestrator/scripts/dispatch.py:935)).

2. The repository wrapper maps a child exit code of zero to `PASS` and has no independent successful-skip authorization state ([scripts/test](/home/val/orchestrator/scripts/test:22)).

3. SPEC-015 demonstrates the resulting false green:

   - `dispatch_gate4.sh` skipped because PyYAML/jsonschema were absent ([test.log](/home/val/orchestrator/.orchestrator/attempts/SPEC-015/1/test.log:7)).
   - `dispatch_parallel.sh` skipped for the same reason ([test.log](/home/val/orchestrator/.orchestrator/attempts/SPEC-015/1/test.log:10)).
   - `worker_isolation.sh` skipped because on-box prerequisites were absent ([test.log](/home/val/orchestrator/.orchestrator/attempts/SPEC-015/1/test.log:30)).

4. The reviewer received only an aggregate exit-zero statement rather than the omitted test evidence ([scripts/dispatch.py](/home/val/orchestrator/scripts/dispatch.py:1186); [scripts/dispatch.py](/home/val/orchestrator/scripts/dispatch.py:1188)).

5. Current verdict evaluation examines only supplied criterion entries and can accept a supplied all-`MET` subset ([scripts/dispatch.py](/home/val/orchestrator/scripts/dispatch.py:1106)).

6. Current artifact binding checks `spec_digest`, `base_sha`, and `worker_commit`, but not an immutable launch-time acceptance-criteria vector ([scripts/dispatch.py](/home/val/orchestrator/scripts/dispatch.py:1119)).

7. Candidate-controlled diff text is already an untrusted semantic prompt channel ([scripts/dispatch.py](/home/val/orchestrator/scripts/dispatch.py:1141); [scripts/dispatch.py](/home/val/orchestrator/scripts/dispatch.py:1188)).

8. The current parent cannot distinguish a substantive candidate test command from `true`; therefore Dispatches 1–4 require the independently installed bootstrap verifier.

9. A nonce placed in the trusted test environment would label an event but would not authenticate its producer if candidate processes inherited the environment or event descriptor.

10. A requirements-file-only venv address is insufficient across Python ABI and platform changes, and `PYTHONNOUSERSITE` alone does not prevent candidate-root import shadowing.

11. Atomic rename provides crash consistency but not no-replace evidence creation.

12. The existing approval path convention does not by itself provide the new attempt/test-blob bindings required here; the separate approval-schema plan is therefore a prerequisite.

### 2.2 Assumptions

1. `scripts/requirements.txt` remains the trusted direct dependency input. This plan does not change dependency versions merely to implement the gate.

2. The operator can install root-owned files below `/srv/codexwork/bootstrap`, `/srv/codexwork/installed`, `/srv/codexwork/venvs/gate`, and `/srv/codexwork/activations`.

3. The separately planned approval-schema work can represent and validate the exact bindings required by R9.

4. The root-owned bootstrap verifier can invoke candidate code through the existing `codex-worker` isolation boundary without exposing operator credentials.

5. Parent-owned trust tests can be adapted so every candidate process launch passes through the parent-owned execution helper. A test that launches candidate code outside that helper is ineligible to authorize a candidate.

6. Git commit and tree objects for the approved base, candidate, and installed parent remain available for snapshot materialization and tree comparison.

7. Historical raw evidence may be absent from a clean CI checkout. The real SPEC-015 replay is therefore an on-box validation; a tracked synthetic fixture is only a portable companion test.

8. A machine can prove exact assertion execution, provenance, and binding. It cannot prove that the assertions are semantically sufficient; high-assurance review and human authorization remain required.

9. Full transitive dependency hash locking is outside this plan. The venv improvements in R6 reduce identity ambiguity and shadowing without claiming a hermetic supply-chain proof.

10. Any drift in cited symbols before authorization requires citation refresh and renewed challenge.

### 2.3 Repository-access note

No new path-and-line citations were introduced in revision 3. The citations above are the live locations repeated in both revision 2 and the independent critique. Draft-time shell revalidation was attempted but blocked before command execution by `bwrap: loopback: Failed RTM_NEWADDR`; authorization must refresh these citations if the referenced symbols have moved.

## 3. Requirements & acceptance criteria

### R1. Independently installed bootstrap verification and activation state

Before Dispatch 1, the operator must install a frozen bootstrap verifier and inventories outside candidate-controlled content. The verifier must gate Dispatches 1–4, validate their candidate commits through the worker boundary, provision trusted installed snapshots, and issue strict activation manifests. Dispatch 4’s activation must be authorized by this prior frozen verifier, not by Dispatch 4’s own new policy or tests.

Acceptance criteria:

1. **R1.1:** `/srv/codexwork/bootstrap/PLAN-003/<verifier-sha256>/` contains the root-owned verifier, strict schema, frozen Dispatch 1–4 test profiles, and exact expected assertion IDs; the directory is not writable by `codex-worker`.
2. **R1.2:** The verifier and inventory hashes are bound by the approval artifact created under the prerequisite approval-schema plan.
3. **R1.3:** A Dispatch 1, 2, or 3 candidate that replaces `scripts/test`, a test helper, or its declared `test_command` with an exit-zero stub is rejected by the frozen verifier.
4. **R1.4:** A Dispatch 4 candidate cannot activate from its own policy selection or self-run alone; the activation manifest records a passing frozen-verifier result produced by Dispatch 0.
5. **R1.5:** `/srv/codexwork/activations/<commit>.json` is required before a parent can dispatch. Missing, malformed, duplicate-keyed, mismatched, writable, or wrong-owner manifests cause refusal.
6. **R1.6:** The activation manifest binds the activated commit, previous activated commit, installed-snapshot manifest hash, bootstrap/native verifier hash, inventory hash, venv identity, required on-box drill results, approval digest, and exact integration-tip observation.
7. **R1.7:** An activation attempt without a prior-parent/bootstrap validation artifact fails even when the candidate’s own tests pass.
8. **R1.8:** CI may emit a portable validation artifact, but activation still fails when the required on-box worker-isolation result is absent.
9. **R1.9:** The activation checker rejects an intervening, unenumerated integration commit between the recorded previous parent and proposed active commit.
10. **R1.10:** After Dispatch 4 activates, its native dispatcher independently requires a valid activation manifest before Dispatch 5 or any later dispatch.

### R2. Exact required-test and assertion attestation with authenticated production

The parent policy must map every required test and execution lane to an exact expected assertion-ID set. Required authorization requires exact-set equality and controller-authenticated events.

Acceptance criteria:

1. **R2.1:** Every required test is launched exactly once from trusted parent content and has exactly one final trusted result.
2. **R2.2:** A required test that emits one trivial assertion while bypassing any expected substantive assertion fails with `missing_assertions`.
3. **R2.3:** A missing, unknown, or duplicate assertion ID fails closed; assertion counts alone are never sufficient.
4. **R2.4:** Every TRUST-class substantive assertion is mutation-tested to produce `ok: false` and nonzero status, or has a specific per-assertion written justification approved with the test inventory.
5. **R2.5:** Representative-per-idiom mutation testing is permitted only for non-TRUST tests and does not replace R2.4.
6. **R2.6:** Candidate application code attempting to forge trusted events through inherited environment, inherited descriptors, `/proc/<ppid>/environ`, or `/proc/<ppid>/fd` cannot emit an accepted trusted event.
7. **R2.7:** Trusted and supplemental invocations use distinct pipes, per-invocation nonces, source-class bindings, and controller state. A race using the same test ID cannot cross namespaces.
8. **R2.8:** `test_command: true`, an exit-zero command with no assertions, or free-form test output is recorded as non-attested supplemental command output, never as successful test evidence and never as satisfaction of a required ID.
9. **R2.9:** Candidate processes receive neither the trusted nonce nor any assertion pipe descriptor. The helper scrubs attestation variables and closes all non-whitelisted descriptors before every candidate `exec`.
10. **R2.10:** The trusted controller alone converts observed command outcomes to events; candidate-written JSON is not admitted as trusted evidence.
11. **R2.11:** A side-effect counter proves each required test was launched once, including under parallel execution; launching twice and retaining one result fails.
12. **R2.12:** Candidate-modified tests run only as supplemental tests. A supplemental process emitting trusted IDs, trusted nonces, or trusted source class fails without satisfying a required result.
13. **R2.13:** A required skip, zero expected assertions, absent trusted test, malformed event, stale nonce, wrong binding, nonzero exit, inconsistent overall result, or missing aggregate yields `failed_test` before review.
14. **R2.14:** The real SPEC-015 replay enters through the production gate state machine, produces `legacy_missing_attestation`, and leaves reviewer and PR spies at zero calls.
15. **R2.15:** A constructed aggregate cannot independently authorize an attempt; the gate requires the invocation records and controller-produced result from the real isolated process.
16. **R2.16:** Candidate-added tests remain supplemental until merged, installed, validated by the prior active verifier, and included in a later parent inventory.

### R3. Trusted closure, canonical diff, and candidate-tree binding

The authoritative gate closure must come from a root-owned installed snapshot of the exact parent commit. Policy classification must use immutable Git trees. The candidate checkout must be clean and equal to `worker_commit` at every transition named below.

Acceptance criteria:

1. **R3.1:** `/srv/codexwork/installed/<commit>` is materialized from the exact Git commit and contains a strict closure manifest covering dispatcher, wrapper, runner, policy, tests, helpers, fixtures, verdict/spec schemas, bootstrap/native verifier inputs, and interpreter identities.
2. **R3.2:** Snapshot files and directories are root-owned, non-worker-writable, regular where required, and manifest-bound before use.
3. **R3.3:** A candidate program or supplemental command that mutates tracked or untracked worktree content after emitting PASS causes the post-test revalidation to return `failed_test`.
4. **R3.4:** The dispatcher revalidates clean status and exact tree identity immediately before reviewer invocation; mismatch produces `failed_review` and no reviewer call.
5. **R3.5:** The dispatcher repeats the revalidation immediately before PR creation; mismatch produces `failed_review` and no PR call.
6. **R3.6:** Review diff and policy classification derive from the bound Git commit trees, not mutable worktree bytes.
7. **R3.7:** Concurrent replacement of a test, runner, policy, helper, fixture, schema, directory component, or interpreter between verification and execution fails; the verified component is not reopened through an untrusted pathname.
8. **R3.8:** Required shell content executes through a verified Bash identity and an already-open script descriptor; closure support files are supplied through verified descriptors or dispatcher-owned immutable invocation material.
9. **R3.9:** Drift in installed runner, policy, schema, helper, fixture, wrapper, or interpreter from the closure manifest prevents dispatch.
10. **R3.10:** Canonical policy comparison covers content, executable/mode changes, symlink and object-type changes, submodules, `.gitmodules`, additions, deletions, and both paths of any inferred rename.
11. **R3.11:** Policy authorization does not depend on rename heuristics: the complete base-tree and candidate-tree path maps are compared, so delete/add rename evasion still yields both path changes.
12. **R3.12:** Every changed path property-tests to either a nonempty parent-required test set or a policy error.
13. **R3.13:** Unknown root paths, generated control-plane configuration, dirty index state, and untracked files in control-plane areas fail closed.
14. **R3.14:** A candidate changing `scripts/dispatch.py` while declaring `test_command: true` is still classified from the immutable tree diff and receives the full parent-selected trust suite.

### R4. Verdict v4 parsing, immutable criterion provenance, and positional binding

Verdict authorization must use strict JSON parsing and the digest-approved criteria vector persisted before candidate work.

Acceptance criteria:

1. **R4.1:** Launch-time attempt creation opens and validates the approved spec once, persists its exact ordered acceptance-criteria vector and vector digest using no-replace creation, and never reopens the mutable spec path for review authorization.
2. **R4.2:** A concurrent spec-path replacement after launch cannot change the criteria used for verdict validation.
3. **R4.3:** JSON parsing rejects duplicate top-level or nested object keys, including duplicate `verdict`, binding, `severity`, `criterion_index`, or `result` keys.
4. **R4.4:** Parsing rejects `NaN`, positive/negative infinity, and all other nonstandard JSON constants.
5. **R4.5:** For an N-criterion vector, only exactly N entries in exact positional order with sequential one-based indices may authorize PASS.
6. **R4.6:** Missing, extra, duplicated, reordered, out-of-range, or invented criteria reject before binary evaluation.
7. **R4.7:** NFKC and whitespace normalization remains exactly as defined in §4.9; case, punctuation, Markdown, or wording changes reject.
8. **R4.8:** Wrong `spec_digest`, `base_sha`, `worker_commit`, criteria-vector digest, or activation-parent commit rejects.
9. **R4.9:** v1–v3 verdicts remain displayable through enumerated legacy consumers but cannot authorize a new or resumed gate decision.
10. **R4.10:** A complete consumer inventory proves that replacing the authorization schema does not silently break historical display-only readers.

### R5. Structured blocking findings

Verdict schema v4 must use strict structured scope, regression, and security finding arrays and the defensive blocker predicate retained from revision 2.

Acceptance criteria:

1. **R5.1:** An all-`MET` PASS with a blocker in any finding domain is rejected.
2. **R5.2:** Empty arrays or advisory-only arrays remain eligible if all other bindings pass.
3. **R5.3:** Unknown, null, empty, numeric, misspelled, or inferred severities reject; allowed values are exactly `advisory` and `blocker`.
4. **R5.4:** Direct calls to `evaluate_binary_review()` reject malformed findings even without prior schema validation.
5. **R5.5:** The top-level object, criterion objects, and finding objects reject undeclared properties.
6. **R5.6:** Review rejection always leaves the PR-opening spy at zero calls.

### R6. ABI/platform-addressed isolated gate environment and provisioning

The trusted venv identity must include the trusted requirements bytes and Python ABI/platform. Trusted Python must run isolated from the candidate checkout.

Acceptance criteria:

1. **R6.1:** The venv address is `/srv/codexwork/venvs/gate/<venv-id>`, where `venv-id` is a digest of the trusted requirements bytes, Python implementation/cache tag or SOABI, normalized platform/machine identity, and the provisioning-format version.
2. **R6.2:** A Python ABI or platform change produces a distinct address and does not collide with an existing environment.
3. **R6.3:** Trusted Python runs with `-I`, a fixed safe cwd outside the candidate tree, a scrubbed `PYTHON*` environment, and no user-site or candidate-root import path.
4. **R6.4:** `yaml` and `jsonschema` module origins are verified to resolve beneath the selected venv before required tests run.
5. **R6.5:** Candidate `yaml.py`, `jsonschema.py`, `sitecustomize.py`, `usercustomize.py`, or similarly shadowing modules do not load into the trusted process.
6. **R6.6:** Recursive ownership, ACL, symlink, and writability checks prove `codex-worker` can read/execute but cannot create, replace, remove, rename, chmod, or redirect venv content.
7. **R6.7:** Missing, stale, mismatched, writable, shadowed, or unusable environments block before required tests and review, with no fallback.
8. **R6.8:** A fresh-box test exercises the documented `scripts/setup-worker-user.sh` provisioning path for worker UID, installed snapshot, venv, systemd properties, ACLs, and activation manifest.
9. **R6.9:** A Python-version-change/idempotence test preserves the old environment, creates the correctly distinct new identity, and activates only after its verification.
10. **R6.10:** Candidate requirements are never installed pre-merge. After merge, the previous frozen/native verifier provisions and validates the new venv from the newly installed merged-parent snapshot before activation.
11. **R6.11:** CI-local venv creation is labelled portable validation and never claims to prove the on-box provisioning path.

### R7. Bounded reviewer evidence with syntactic integrity only

Machine-generated attestation summaries are primary reviewer evidence. A bounded sanitized log tail may be secondary untrusted evidence.

Acceptance criteria:

1. **R7.1:** Raw test output capture is capped at 16 MiB before hashing or prompt construction. Exceeding the cap terminates capture, fails the test gate with `log_limit_exceeded`, and does not invoke review.
2. **R7.2:** The prompt includes the trusted required-test/assertion summary before the log tail.
3. **R7.3:** The retained tail is limited to the final 16 KiB and final 80 logical lines.
4. **R7.4:** Escaping, control removal, and fixed delimiters guarantee only syntactic prompt integrity: log bytes cannot close or restructure the evidence container.
5. **R7.5:** The plan and tests make no claim that instruction-like content cannot semantically influence the reviewer.
6. **R7.6:** The reviewer remains an untrusted fail-closed oracle layered after deterministic scope, tree, policy, test, assertion, and binding gates.
7. **R7.7:** Missing, symlinked, replaced, raced, unreadable, or over-limit log evidence blocks review.
8. **R7.8:** Log bytes never appear in process arguments or shell interpolation; the prompt is provided through stdin or a dispatcher-owned prompt file.

### R8. Correct metrics semantics and approval-bound extraction parity

Metrics extraction must preserve pre-candidate behavior; semantic correction must classify completed specs once and use a defined window anchor.

Acceptance criteria:

1. **R8.1:** Before Dispatch 1 candidate work, the operator captures baseline text, JSON, ordering, defaults, errors, and exit statuses from the installed parent; their hashes and capture commands are approval-bound.
2. **R8.2:** Candidate-authored parity fixtures cannot replace, update, or bless the operator-captured baseline.
3. **R8.3:** Dispatch 1 matches the approval-bound baseline byte-for-byte or field-for-field except enumerated module-origin metadata.
4. **R8.4:** Non-metrics commands do not import or depend on `scripts/metrics_report.py`.
5. **R8.5:** All actor-attributing unmatched-merge labels become exactly `not_attributed_to_orchestrator`, with actor identity stated as unknown.
6. **R8.6:** The rate denominator is distinct eligible completed specs; the numerator is specs whose first and only attempt reached normal `merged` after all gates.
7. **R8.7:** Eligibility is anchored by the canonical final spec terminal timestamp in the selected half-open window `[start, end)`.
8. **R8.8:** Classification consults the spec’s complete retained attempt history, including attempts before the window and later attempts after it. A pre-window retry prevents straight-through classification; a later attempt moves terminal anchoring to the later final completion.
9. **R8.9:** The five-spec fixture produces numerator `1`, denominator `3`, rate `1/3`, excluded-in-progress `1`, and malformed/incomplete `1`.
10. **R8.10:** Adding attempts to a retrying spec does not change numerator or denominator except when it changes the canonical final terminal state/time.
11. **R8.11:** Zero denominator produces JSON `null` and display `N/A`.
12. **R8.12:** Dispatch 2 cannot modify dispatcher, gate-transition, authorization, attempt-serialization, activation, or test-policy paths.
13. **R8.13:** Phase 2 remains blocked after Dispatch 2 and unblocks only after R10.4.

### R9. Scoped no-replace evidence creation and approval dependency

PLAN-003 must implement the B6 subset assigned here and depend on the separate plans for the rest.

Acceptance criteria:

1. **R9.1:** Every new attestation aggregate, criteria vector, review request, review result, and activation-related attempt artifact is created once with `O_CREAT|O_EXCL` or an equivalent no-replace primitive.
2. **R9.2:** Final evidence paths and parent directories are dispatcher-owned and not writable by the worker or candidate.
3. **R9.3:** Worker output crosses bounded pipes; candidate processes receive no writable final evidence pathname.
4. **R9.4:** Existing destinations, symlinks, hardlink aliases, rename swaps, concurrent writers, and resume-after-terminal attempts cannot replace a finalized aggregate, log, or review result.
5. **R9.5:** A finalized attempt is never reopened for authorization writes. Resume requires a new attempt number or explicit non-authorizing display access.
6. **R9.6:** SPEC-015 bytes and hashes remain unchanged after the real replay.
7. **R9.7:** Full artifact sealing, directory `fsync` policy, terminal hash manifests, and comprehensive retained-evidence integrity remain assigned to the separate `evidence-hashing` plan.
8. **R9.8:** The prerequisite approval-schema plan validates exact dispatch, attempt, candidate digest, verifier/inventory digest, parity-baseline digest, expected test/assertion inventory, and every changed test’s old/new blob identity.

### R10. Compatibility, dependency, and Phase 2 sequence

Test evolution and dependency upgrades must not rely on self-promotion.

Acceptance criteria:

1. **R10.1:** An ordinary compatible test implementation change passes both the installed parent test and the candidate supplemental test before later promotion.
2. **R10.2:** A behavior change that contradicts the old strict test uses the complete three-state sequence in §4.7; each stage is a separate normal high-assurance dispatch and activation.
3. **R10.3:** An end-to-end test exercises all three states and proves that no stage’s candidate test is authoritative during its own attempt.
4. **R10.4:** Phase 2 implementation dispatch refuses until Dispatch 0 is installed and Dispatches 1–5 are merged, installed, activation-manifested, and validated.
5. **R10.5:** If a compatibility bridge is impossible, the normal pipeline fails closed pending separately scoped operator bootstrap authority; this plan grants no such authority.

## 4. Design / approach

### 4.1 Dispatch decomposition and dependency order

The approval-schema plan from baton action 4 is an external prerequisite and must be active before this sequence.

| Order | Dispatch | Lane | Dependency | Purpose |
|---:|---|---|---|---|
| 0 | Bootstrap verifier, trusted snapshots, and activation manifests | operator-installed high-assurance bootstrap | Approval-schema plan active | Install the frozen non-candidate verifier and inventories; provision `/srv/codexwork/installed/<commit>`; gate Dispatches 1–4; create machine-checked activation state. |
| 1 | Metrics extraction | high-assurance | Dispatch 0 installed; current parent activated | Extract reporting while preserving approval-bound baseline behavior. |
| 2 | Metrics semantics | ordinary | Dispatch 1 merged, frozen-verified, installed, activated | Correct attribution and straight-through semantics without dispatcher write scope. |
| 3 | Assertion-event migration | high-assurance | Dispatch 2 merged and activated | Migrate tests to exact assertion IDs, exit-77 diagnostics, source/target separation, and per-assertion mutation proof. |
| 4 | Attestation, trusted execution, venv, activation enforcement, tree binding, and reviewer evidence | high-assurance | Dispatch 3 merged, frozen-verified, installed, activated | Install the real parent gate under the frozen verifier and make activation enforcement native. |
| 5 | Verdict v4 and blocking findings | high-assurance | Dispatch 4 activated by Dispatch 0 artifact and native activation check | Add strict JSON parsing, launch-time criteria vector, positional v4 binding, and blocker evaluation. |

“Completed” means merged where repository content exists, exact installed snapshot provisioned, no intervening commit accepted, required verifier passed, and activation manifest accepted.

### 4.2 Per-dispatch allowed paths

All repository allowances must expand to literal paths before authorization.

**Dispatch 0 — operator-installed bootstrap outputs:**

- `/srv/codexwork/bootstrap/PLAN-003/<verifier-sha256>/verify.py`
- `/srv/codexwork/bootstrap/PLAN-003/<verifier-sha256>/bootstrap.schema.json`
- `/srv/codexwork/bootstrap/PLAN-003/<verifier-sha256>/inventory.json`
- `/srv/codexwork/bootstrap/PLAN-003/<verifier-sha256>/bootstrapctl`
- `/srv/codexwork/installed/<commit>/`
- `/srv/codexwork/activations/<commit>.json`

Dispatch 0 is installed from an operator-authorized frozen artifact, not a worker candidate. It may read the repository and Git object database but cannot silently edit tracked repository files.

**Dispatch 1 — metrics extraction:**

- `scripts/dispatch.py`, limited to removing metrics implementation and adding the lazy alias
- `scripts/metrics_report.py`
- `tests/metrics_report.sh`
- `tests/dispatch_gate4.sh`, limited to alias parity and non-metrics import isolation

**Dispatch 2 — metrics semantics:**

- `scripts/metrics_report.py`
- `scripts/delegation_report.py`
- `tests/metrics_report.sh`
- `tests/delegation_report.sh`
- `tests/fixtures/metrics_semantics.json`

**Dispatch 3 — assertion migration:**

- `scripts/test`
- `scripts/lib/test_attest.sh`
- `tests/assertion_migration.sh`
- The literal authorization-time output of `git ls-files 'tests/*.sh'`

Dispatch 3 may not change gate transitions, policy, venv setup, activation enforcement, verdict handling, or reviewer construction.

**Dispatch 4 — repaired test gate:**

- `scripts/dispatch.py`, limited to test selection, invocation, aggregation, tree checks, activation checks, evidence capture, and reviewer construction
- `scripts/test`
- `scripts/test_runner.py`
- `scripts/test-policy.json`
- `scripts/setup-worker-user.sh`
- `tests/dispatch_gate4.sh`
- `tests/dispatch_parallel.sh`
- `tests/worker_isolation.sh`
- `tests/test_attestation.sh`
- `tests/reviewer_evidence.sh`
- `tests/activation_manifest.sh`
- `tests/trusted_snapshot.sh`
- `tests/tree_binding.sh`
- `tests/fixtures/verdict_integrity/spec015_skips.log`
- `.github/workflows/ci.yml`

**Dispatch 5 — verdict v4:**

- `scripts/dispatch.py`, limited to strict review parsing, criteria-vector persistence/binding, v4 evaluation, and prompt schema instructions
- `scripts/verdict.schema.json`
- `tests/dispatch_gate4.sh`
- `tests/verdict_integrity.sh`
- `tests/fixtures/verdict_v4.json`

### 4.3 Dispatch 0 bootstrap verifier

Dispatch 0 is a small frozen verifier, not a duplicate production dispatcher. Its inventory has four immutable profiles, one for each of Dispatches 1–4. Each profile specifies:

- exact allowed repository paths;
- exact bootstrap test IDs;
- exact expected assertion IDs per test;
- immutable test/helper content hashes;
- candidate command entry points;
- required worker-boundary properties;
- activation drills required after merge;
- expected predecessor active commit;
- required snapshot-closure components.

The verifier runs from `/srv/codexwork/bootstrap`, never from the candidate checkout. It invokes candidate components as data through the `codex-worker` boundary. Its assertions are computed by the frozen controller from observed results; candidate JSON does not count.

For Dispatches 1–4, the operator-facing bootstrap launcher is the required invocation path. It refuses an absent or mismatched current-parent activation manifest. After a candidate is merged, it verifies the exact integration commit, rejects intervening commits, provisions its installed snapshot, runs the frozen post-merge profile, and creates the activation manifest with no-replace semantics.

Dispatch 4 must pass both its candidate-phase frozen profile and its post-merge frozen activation profile. Its own new tests are supplemental to this activation decision.

### 4.4 Installed snapshot and race-safe closure

Snapshot provisioning materializes the exact Git commit into `/srv/codexwork/installed/<commit>` without using a mutable checkout as the final source. The provisioner:

1. Resolves the commit and tree objects.
2. Enumerates the full gate closure.
3. Copies each expected blob into a new root-owned staging directory using no-follow creation.
4. Records repository path, mode, object ID, content hash, size, and role.
5. Records verified identities for Bash, trusted Python, and any other interpreter.
6. Removes worker write access recursively.
7. Atomically installs the previously absent commit directory.
8. Verifies the installed closure against the manifest before activation.

The native runner pins a directory descriptor and resolves closure components beneath it using no-follow, beneath-only semantics. Native executable components use already-open descriptors where supported. Required shell tests are passed to the verified Bash interpreter through an already-open script descriptor; helpers and fixtures are pre-opened or copied from verified descriptors into a dispatcher-owned, non-worker-writable invocation bundle. No verified candidate-writable pathname is later reopened for execution.

Concurrent replacement tests target files and directory components, including helpers, fixtures, schemas, and interpreter shadows. Worker inability to write the root-owned snapshot is necessary but does not replace closure verification.

### 4.5 Canonical Git change model and required-test policy

Policy evaluation compares the complete path maps of `base_sha^{tree}` and `worker_commit^{tree}`. Each path state includes Git mode, object ID, and object type. Rename detection may be recorded for display, but requiredness is calculated from the underlying additions, deletions, and modifications, so rename thresholds cannot suppress coverage.

The policy must cover:

- regular-file content and mode changes;
- symlink targets and regular/symlink type changes;
- executable-bit changes;
- gitlinks/submodules and `.gitmodules`;
- additions and deletions;
- both sides of displayed renames;
- unknown paths at repository root;
- generated control-plane configuration;
- unrecognized paths below `scripts/`, `tests/`, `specs/`, `.github/workflows/`, and `.orchestrator/`.

Every changed path yields a nonempty required-test set or a policy error. Candidate policy changes never affect their own attempt.

Worktree state is a separate transition invariant. Untracked or dirty control-plane content fails even if it is absent from the committed tree.

### 4.6 Assertion-producer boundary and attestation protocol

The installed parent controller owns:

- the trusted and supplemental pipes;
- all per-invocation nonces;
- expected source class;
- exact expected assertion IDs;
- launch count;
- command outcome observation;
- aggregate construction;
- final evidence paths.

Trusted tests describe assertions and candidate commands through the parent-owned helper. For a candidate command, the controller/helper:

1. Validates the requested assertion ID against the active expected set.
2. Forks a candidate process through the worker boundary.
3. Sets a fixed candidate-safe environment without nonce or attestation variables.
4. Scrubs `PYTHONPATH`, `PYTHONHOME`, `PYTHONSTARTUP`, and related variables.
5. Closes all descriptors except the explicit stdin/stdout/stderr and command-specific data descriptors.
6. Makes the trusted controller non-dumpable where supported and applies the on-box `/proc`/ptrace restrictions validated by the attack tests.
7. Observes the command outcome.
8. Produces the assertion event itself.

The event schema is:

```json
{
  "version": 1,
  "run_nonce": "<controller nonce>",
  "source_class": "trusted",
  "test_id": "tests/example.sh",
  "assertion_id": "example.stable.condition",
  "sequence": 1,
  "event": "assertion",
  "ok": true
}
```

The exact assertion-ID set must equal the parent inventory set. Names or free-form text may be stored diagnostically but are not identities.

Trusted and supplemental invocations receive independent pipes and nonces. Supplemental results are stored only under `candidate_supplemental_results`; they can block on failure but can never satisfy `trusted_required_results`.

The aggregate records parent snapshot commit and manifest hash, policy hash, base and candidate tree IDs, expected and observed assertion-ID sets, invocation count, source class, exit status, and overall status.

### 4.7 Candidate tree transitions and three-state compatibility

The candidate worktree is revalidated:

1. After all trusted, supplemental-test, and free-form candidate commands complete.
2. Immediately before reviewer invocation.
3. Immediately before PR creation.

Each check requires the expected `HEAD`, exact committed tree identity, clean index/tracked state, and no forbidden untracked content. Review and PR inputs use commit-object data.

A behavior change that contradicts the installed strict test uses three states:

1. **State A — bridge plus transitional test.** The old strict parent test remains authoritative. The candidate adds compatibility behavior and a transitional test that accepts the old and new behavior. Both old strict and supplemental transitional tests pass. After frozen/native verification, the transitional test becomes parent content.
2. **State B — strict-new test promotion.** With the transitional test authoritative and the bridge still present, a second dispatch adds the strict-new test. The supplemental strict-new test passes. After activation, the strict-new test becomes parent content.
3. **State C — bridge removal.** With the strict-new test authoritative, a third dispatch removes the compatibility bridge. The authoritative strict-new test must pass.

No candidate test is authoritative during its own attempt. If any state is impossible, the sequence stops and requires external bootstrap authority.

### 4.8 Activation manifest

The strict activation manifest contains:

```json
{
  "schema_version": 1,
  "commit": "<activated commit>",
  "previous_active_commit": "<prior commit>",
  "integration_tip": "<observed exact tip>",
  "approval_digest": "<approval artifact digest>",
  "installed_snapshot": "/srv/codexwork/installed/<commit>",
  "snapshot_manifest_sha256": "<digest>",
  "verifier_sha256": "<bootstrap or native verifier digest>",
  "inventory_sha256": "<required test/assertion inventory digest>",
  "venv_id": "<ABI/platform-addressed identity>",
  "portable_validation": {"status": "PASS", "artifact_sha256": "<digest>"},
  "on_box_validation": {"status": "PASS", "artifact_sha256": "<digest>"},
  "created_at": "<timestamp>"
}
```

The schema rejects undeclared fields and duplicate keys. The file is root-owned, non-worker-writable, and created only if absent. Before dispatch, the active dispatcher validates every bound digest against installed state.

### 4.9 Verdict v4 criterion normalization and binding

At attempt launch, the dispatcher persists the exact ordered criteria vector from the digest-approved spec. It records raw text, normalized text, position, and vector digest. Review authorization never rereads the spec path.

Normalization remains:

1. Unicode NFKC.
2. CRLF and CR to LF.
3. Strip leading and trailing Unicode whitespace.
4. Replace each remaining maximal Unicode-whitespace run with one ASCII space.

There is no case folding, punctuation removal, Markdown rendering, link resolution, fuzzy matching, or clause reordering.

Validation order is:

1. Size bound.
2. Strict JSON parse with duplicate-key and nonstandard-constant rejection.
3. Schema v4.
4. Artifact and activation-parent bindings.
5. Criteria-vector digest.
6. Exact count.
7. Sequential indices.
8. Positional normalized text equality.
9. Binary review evaluation.

### 4.10 Structured findings and blocker predicate

The retained blocker predicate is:

```text
blocking =
    findings is not an array
    OR any entry is not an object
    OR any required field is absent or invalid
    OR any undeclared field is present
    OR severity is not exactly "advisory" or "blocker"
    OR any severity is exactly "blocker"
```

An empty array is the only “no findings” representation. `evaluate_binary_review()` rechecks the predicate even after schema validation.

### 4.11 Gate venv and staged dependency upgrade

The venv identity is a digest over a canonical record containing:

- hash of the trusted parent `scripts/requirements.txt` bytes;
- Python implementation and ABI/cache tag or SOABI;
- normalized OS/platform/machine tuple;
- provisioning-format version.

Trusted Python is invoked with `-I`, from a fixed safe cwd, with Python variables scrubbed. Direct dependency imports and their resolved origins are verified beneath the selected venv.

A candidate requirements change is staged:

1. The candidate is tested using the active parent’s dependency environment; candidate requirements are not installed.
2. After merge, the new commit is materialized as an inactive installed snapshot.
3. The previous frozen/native verifier provisions a new ABI/platform-addressed venv from that merged snapshot.
4. The verifier runs origin, isolation, and on-box tests.
5. Only then may an activation manifest name the new venv and commit.

### 4.12 Reviewer evidence

The controller enforces the raw-log limit while reading process output. It never permits an unbounded file and hashes only the bounded captured artifact.

The reviewer receives:

1. Fixed instructions.
2. Machine-generated activation, tree, required-test, expected/observed assertion, and binding summaries.
3. A secondary JSON-escaped log tail labelled untrusted.
4. Fixed instructions after the evidence block.

Escaping protects syntax only. Mechanically decidable properties are resolved before reviewer invocation and are never delegated to model judgment.

### 4.13 Metrics extraction and definitions

Dispatch 1 parity compares against operator-captured, approval-bound baselines created before the candidate branch. The candidate may add regression tests but cannot redefine the baseline.

Dispatch 2 groups complete attempt history by spec. A spec’s canonical final terminal result determines its terminal timestamp. Window eligibility is based on that timestamp in `[start, end)`, while classification uses all retained attempts regardless of their timestamps.

- **Straight-through:** attempt 1 is the only attempt, reached normal `merged`, and retained evidence shows all normal gates passed.
- **Completed but not straight-through:** final terminal completion exists, but there was a retry, replacement, override, first-attempt failure, or later attempt.
- **In progress:** no canonical final terminal completion.
- **Malformed/incomplete:** safe classification is impossible.

A retry before the selected window still prevents straight-through status for an in-window completion. A later attempt after the window means the spec’s final terminal anchor is later and the earlier result does not independently enter the earlier denominator.

### 4.14 Scoped evidence handling

The dispatcher creates new authorization artifacts only at dispatcher-owned paths with no-replace semantics. Candidate output is consumed through pipes. Finalized attempts refuse new authorization writes.

This plan intentionally stops short of defining the full evidence seal and hash-chain lifecycle. That work remains in the separate `evidence-hashing` plan.

### 4.15 Alternatives considered

1. **Continue with the existing gate until Dispatch 4.** Rejected because Dispatches 1–3 and Dispatch 4 activation would remain self-gated.
2. **Build a second complete production gate.** Rejected as unnecessary scope; Dispatch 0 is a frozen minimal verifier.
3. **Require merely one assertion.** Rejected because partial execution remains vacuous.
4. **Allow candidate assertion JSON with a nonce.** Rejected because inherited secrets and descriptors do not authenticate the producer.
5. **Make free-form `test_command` attestation evidence.** Rejected unless it is converted into a parent-owned ID/assertion contract; revision 3 treats it as non-evidentiary supplemental output.
6. **Require a candidate read-only bind mount.** Deferred as optional hardening; mandatory transition-time tree revalidation provides the scoped revision 3 invariant.
7. **Execute a verified shell script by reopening its pathname.** Rejected because it reintroduces a TOCTOU.
8. **Run trusted content from the operator’s home checkout.** Rejected because D5 intentionally makes the operator home unreachable to workers.
9. **Treat atomic rename as evidence immutability.** Rejected; new authorization artifacts use no-replace creation.
10. **Duplicate full evidence sealing here.** Rejected because it belongs to the separate evidence-hashing plan.
11. **Claim reviewer log escaping prevents prompt injection.** Rejected; it provides syntactic containment only.
12. **Address venvs only by requirements hash.** Rejected because Python ABI/platform changes can collide.
13. **Add full wheelhouse hash locking now.** Deferred to dependency/supply-chain work.
14. **Rely on rename detection for policy coverage.** Rejected; policy compares complete tree path maps.
15. **Weaken R7.6 to the existing approval invariant.** Rejected. The approval-schema plan is a prerequisite instead.
16. **Reparse the spec at review time.** Rejected; the exact criteria vector is persisted at launch.
17. **Allow JSON duplicate keys and trust the last value.** Rejected as ambiguous authorization input.
18. **Unblock Phase 2 after metrics alone.** Rejected; Phase 2 requires all six Phase 1 dispatches.
19. **Use the revision 2 two-stage compatibility bridge.** Rejected as incomplete; revision 3 specifies the full three-state sequence.

## 5. Affected boundaries & consumers

| Boundary or consumer | Effect |
|---|---|
| Approval-schema plan | Becomes an external prerequisite for exact bootstrap, attempt, baseline, inventory, and changed-test bindings. |
| Operator bootstrap procedure | Installs frozen verifier content outside the repository candidate path. |
| `/srv/codexwork/installed/<commit>` | Becomes the worker-readable, root-owned source of trusted gate closure. |
| Activation state | Becomes a strict manifest checked before dispatch under a parent. |
| Installed parent dispatcher | Becomes the authority for policy, authenticated test orchestration, aggregation, transition checks, v4 parsing, and blockers after Dispatch 4. |
| Bootstrap verifier | Gates Dispatches 1–4 and independently authorizes Dispatch 4 activation. |
| Candidate worktree | Supplies targets and supplemental content only; it is revalidated at three transitions. |
| Candidate application process | Receives no nonce or event descriptor and cannot emit trusted assertions. |
| `scripts/test` users | Receive exact assertion-ID semantics and explicit PASS/SKIP/FAIL diagnostics. |
| Existing `tests/*.sh` | Gain stable assertion IDs and per-assertion TRUST mutation coverage. |
| Candidate test changes | Remain supplemental until later activation. |
| `codex-worker` | Gains read/execute access to installed snapshots and one selected venv, never write access. |
| Gate venv | Becomes ABI/platform-addressed and isolated with `python -I`. |
| Reviewer | Receives machine summaries first and a secondary bounded untrusted log tail. |
| `scripts/verdict.schema.json` | Becomes strict v4 authorization schema. |
| Attempt manifest | Gains immutable criteria vector, vector digest, parent activation, tree IDs, and exact test/assertion inventory. |
| Legacy review consumers | Must be inventoried and remain display-only. |
| Metrics consumers | Receive neutral attribution and spec-level window-anchored semantics. |
| CI | Produces portable validation only; it cannot activate a parent. |
| Phase 2 | Remains blocked until Dispatches 0–5 are all complete and active. |
| Historical SPEC-015 evidence | Remains immutable real-state-machine regression input. |
| Evidence-hashing plan | Retains ownership of full sealing and terminal hash-chain design. |

## 6. Ordered implementation steps

1. Land and activate the separate approval-schema plan, including bindings required by R9.8.
2. Freeze the current parent commit, integration tip, current outputs, SPEC-015 hashes, current test inventory, and current interpreter identities.
3. Author Dispatch 0’s four frozen profiles and exact bootstrap assertion inventories outside candidate content.
4. Independently review and authorize the Dispatch 0 artifact; install it root-owned under `/srv/codexwork/bootstrap`.
5. Provision and verify the current parent snapshot under `/srv/codexwork/installed/<commit>`.
6. Run the initial on-box bootstrap drill and create the current-parent activation manifest.
7. Capture Dispatch 1 metrics parity baselines before candidate work and bind their hashes and capture commands through the approval schema.
8. Add extraction regressions without allowing candidate fixtures to redefine the baseline.
9. Extract metrics into `scripts/metrics_report.py` and retain the lazy alias.
10. Run Dispatch 1 through both the installed parent and frozen bootstrap profile; merge, snapshot, verify, and activate the exact commit.
11. Add Dispatch 2 semantic fixtures, including complete-history and window-boundary cases.
12. Correct neutral attribution labels and per-spec straight-through calculation.
13. Run Dispatch 2 through the frozen verifier; merge, snapshot, and activate it. Record that metrics are ready but Phase 2 remains blocked.
14. Classify every tracked test assertion as TRUST or non-TRUST and assign stable assertion IDs.
15. Create the exact per-test/per-lane expected assertion sets.
16. Add an induced-failure mutation or approved specific justification for every substantive TRUST assertion.
17. Add representative-per-idiom mutation coverage for remaining non-TRUST assertions.
18. Implement the parent helper contract with attestation scrubbing and descriptor closure.
19. Migrate the shell suite and source/target resolution without changing substantive conditions.
20. Run Dispatch 3 through the frozen verifier, including wrapper/helper exit-zero substitution attacks; merge, snapshot, and activate.
21. Add Dispatch 4 failing regressions for producer forgery, exact assertion sets, single launch, namespace races, tree mutation, closure swaps, policy exhaustiveness, evidence no-replace, venv shadowing, activation absence, and real SPEC-015 replay.
22. Implement canonical tree-map classification and parent policy.
23. Implement root-snapshot closure verification and descriptor-based trusted execution.
24. Implement controller-only event production, separate pipes/nonces, environment scrubbing, descriptor closure, `/proc` attacks, and dispatcher-owned aggregation.
25. Add post-test, pre-review, and pre-PR tree revalidation.
26. Implement no-replace creation for new attestation/review artifacts and refusal to reopen finalized attempts.
27. Extend `scripts/setup-worker-user.sh` with snapshot, ABI/platform venv, ACL, systemd, and activation provisioning.
28. Invoke trusted Python with `-I`, safe cwd, scrubbed environment, and verified module origins.
29. Add the 16 MiB capture cap and syntactically safe secondary reviewer log tail.
30. Add native activation-manifest enforcement and the strict installed-commit chain check.
31. Update CI while retaining the single non-matrix job named `ci`; label its result portable-only.
32. Replay real SPEC-015 through the actual state machine and verify reviewer/PR spies remain zero.
33. Run Dispatch 4 candidate validation through the frozen verifier.
34. Merge Dispatch 4, provision its installed snapshot and venv, and run the frozen post-merge activation profile. Do not use Dispatch 4’s own selection as its activation authority.
35. Create and validate Dispatch 4’s activation manifest; only then enable its native gate.
36. Add Dispatch 5 regressions for duplicate keys, constants, launch-time criteria snapshots, spec replacement, positional binding, strict findings, legacy consumer inventory, and PR non-invocation.
37. Persist the criteria vector at attempt launch with no-replace semantics.
38. Implement strict JSON loading, v4 schema, positional binding, and blocker predicate.
39. Run focused and full tests through the activated Dispatch 4 parent.
40. Complete high-assurance challenge, detached SOL review, operator authorization, merge, snapshot provisioning, and activation of Dispatch 5.
41. Exercise the complete three-state compatibility sequence in a controlled fixture.
42. Mark the Phase 2 prerequisite satisfied only after machine verification that all Dispatches 0–5 meet R10.4.

## 7. Failure modes & blast radius

| Trigger | Consequence | Mitigation |
|---|---|---|
| Pre-Dispatch-4 candidate stubs wrapper/helper | Existing gate could remain falsely green | Frozen Dispatch 0 verifier executes independent assertions. |
| One trivial assertion executes | Partial test body is certified | Exact expected assertion-ID set must match. |
| Candidate application forges events | False trusted PASS | No nonce/FD inheritance; controller-only events; `/proc` attack tests. |
| Trusted and supplemental tests race | Supplemental result contaminates trust namespace | Separate pipes, nonces, source class, and controller state. |
| Required test launches twice | Side effects or selective result hiding | Controller launch counter and exact-one requirement. |
| Candidate mutates tested tree | Reviewer sees different content | Post-test, pre-review, and pre-PR tree checks. |
| Parent closure file swaps | Verified content differs from executed content | Root snapshot, closure manifest, pinned descriptors, race tests. |
| Installed parent drifts | Wrong gate implementation runs | Activation and closure digest checks refuse dispatch. |
| Existing artifact is replaced | Authorization history changes | Dispatcher-owned paths, `O_EXCL`, finalized-attempt refusal. |
| Log fills disk or memory | Availability loss before sanitization | 16 MiB capture cap enforced during capture. |
| Reviewer follows log instructions | Advisory judgment may be influenced | Deterministic gates decide mechanics; log is secondary and untrusted. |
| Candidate shadows Python package | Trusted test imports candidate code | `python -I`, safe cwd, scrubbed environment, origin verification. |
| Python upgrade reuses venv path | Wrong environment identity | ABI/platform-addressed venv. |
| Candidate requirements self-provision | Dependency change validates itself | Post-merge staged provisioning by prior verifier. |
| Mode/symlink/submodule change escapes policy | Required coverage disappears | Complete tree-map classification and property tests. |
| Approval lacks test-blob bindings | High-risk change is not authorized as claimed | Approval-schema plan is a hard prerequisite. |
| Duplicate verdict key | Human and machine interpretations differ | Strict duplicate-key rejection. |
| Spec changes after digest check | Wrong criteria authorize PASS | Launch-time immutable criteria vector. |
| CI passes without isolation | Parent activates without on-box proof | Activation manifest requires distinct on-box result. |
| New policy selects its own activation tests | Dispatch 4 self-certifies | Frozen Dispatch 0 verifier authorizes activation. |
| Metrics fixture shifts baseline | Extraction semantic drift is hidden | Operator pre-candidate approval-bound baseline. |
| Retry lies outside selected window | Straight-through rate is misclassified | Terminal-time anchor plus complete-history classification. |
| Phase 2 starts after metrics only | Integrity repair remains incomplete | Machine gate requires all six dispatches. |
| Transitional compatibility test remains weak | Bridge is removed before strict test promotion | Three separate states and activations. |

The worst-case under-strict failure is an incorrectly authorized control-plane change or PR. On detection: stop new dispatches, preserve artifacts, invalidate affected activation, and roll forward under the last trusted verifier. Availability loss from an over-strict gate is preferable to restoring exit-code-only authorization.

## 8. Validation plan

### 8.1 Regression proofs that fail before implementation

1. An exit-zero required skip reaches the current gate’s PASS path.
2. A required test with one trivial assertion omits substantive expected IDs.
3. Candidate application code can observe or target inherited attestation state in a naïve implementation.
4. A candidate command mutates the worktree after PASS.
5. A verified-by-path test/helper can be swapped before execution.
6. A duplicate-key verdict is accepted by a conventional last-key-wins parser.
7. A candidate shadow module loads when Python runs from the target root.
8. Current reporting disagrees with neutral attribution or spec-level window semantics.
9. A legacy SPEC-015 adapter can reject a fixture without exercising the real transition; the new regression demands production state-machine entry and spies.

### 8.2 Dispatch 0 validation

- Verify root ownership and worker write denial for bootstrap content.
- Verify exact verifier and inventory hashes against the approval artifact.
- Run each frozen profile against a known passing fixture and its required mutations.
- Replace candidate wrappers/helpers with exit-zero stubs and require rejection.
- Attempt activation without the frozen result and require refusal.
- Attempt activation with an intervening integration commit and require refusal.
- Attempt Dispatch 4 activation using only its new tests and require refusal.
- Run the fresh-box provisioning path.
- Prove CI portable PASS cannot substitute for the on-box artifact.

### 8.3 Dispatch 1 validation

- Compare output against pre-candidate approval-bound baselines.
- Prove candidate fixtures cannot update the baseline.
- Invoke metrics through alias and standalone module.
- Prove representative non-metrics commands do not import the module.
- Make the module unavailable and prove non-metrics commands are unaffected.
- Run focused tests, `./scripts/test`, and the frozen Dispatch 1 profile.

### 8.4 Dispatch 2 validation

- Prove neutral naming in text, JSON, help, and headings.
- Prove numerator `1`, denominator `3`, and required exclusions.
- Add a retry before the window and prove it prevents straight-through classification.
- Add a later attempt after the window and prove the terminal anchor moves.
- Prove empty denominator is `null`/`N/A`.
- Prove no trust-closure path changed.
- Run `./scripts/test` and the frozen Dispatch 2 profile.

### 8.5 Dispatch 3 validation

- Enumerate every TRUST assertion and its stable ID.
- Force every substantive TRUST condition false and require the matching `ok: false` plus nonzero result, or verify its approval-bound justification.
- Exercise every helper form and representative non-TRUST shell idiom.
- Prove no unconditional pass-recording primitive exists.
- Prove prerequisite absence yields diagnostic exit `77` and no passing assertion.
- Prove source/target separation.
- Replace the candidate wrapper/helper with exit-zero stubs and require frozen-verifier rejection.
- Run full suite, challenge, approval, and frozen Dispatch 3 profile.

### 8.6 Dispatch 4 validation

- Run all R2 malformed and adversarial event cases through the real isolated process.
- Run environment, inherited-FD, `/proc/<ppid>/environ`, and `/proc/<ppid>/fd` forgery attempts.
- Race trusted and supplemental processes with identical test IDs.
- Prove a double launch is detected by the side-effect counter.
- Mutate tracked and untracked target content after PASS.
- Swap tests, helpers, fixtures, schemas, interpreter paths, and directory components concurrently.
- Drift installed runner, policy, schema, and helper from the recorded commit.
- Property-test mode, symlink, type, submodule, `.gitmodules`, delete/add, root-path, untracked, and dirty cases.
- Attempt overwrite via existing file, symlink, hardlink alias, rename swap, concurrent writer, and terminal resume.
- Test candidate `yaml.py`, `jsonschema.py`, `sitecustomize.py`, and `usercustomize.py`.
- Test fresh-box provisioning and Python ABI/version change.
- Enforce raw-log cap before hashing; separately test tail escaping and syntactic delimiter integrity.
- Replay real SPEC-015 through the production state machine with reviewer and PR spies.
- Confirm a constructed aggregate without real invocation records cannot authorize.
- Confirm CI PASS plus missing on-box artifact cannot activate.
- Run Dispatch 4’s candidate and post-merge checks through the frozen verifier.

### 8.7 Dispatch 5 validation

- N correct criteria: eligible.
- N−1, N+1, duplicate, reordered, wrong-index, and invented text: reject.
- Exact and NFKC/whitespace-normalized text: bind.
- Case, punctuation, Markdown, and wording changes: reject.
- Duplicate top-level, criterion, finding, severity, and binding keys: reject.
- Nonstandard constants: reject.
- Replace the spec path after launch and prove the persisted vector remains authoritative.
- Wrong spec, base, worker, vector, or active-parent binding: reject.
- Blocker in each finding domain: reject.
- Empty/advisory arrays: eligible.
- Malformed findings passed directly to evaluator: reject.
- Undeclared properties: reject.
- v1–v3: display-only.
- Inventory and exercise every legacy display consumer.
- Every rejected review leaves PR spy at zero.

### 8.8 Mapping of the critique’s 18 missing tests

| Missing test | Numbered criterion |
|---:|---|
| 1. Pre-Dispatch-4 wrapper/helper stub | R1.3 |
| 2. Candidate application event forgery | R2.6, R2.9, R2.10 |
| 3. One trivial assertion | R2.2, R2.3 |
| 4. `test_command: true` or no assertions | R2.8 |
| 5. Worktree mutation after PASS | R3.3–R3.5 |
| 6. Parent test/helper/fixture swap | R3.7, R3.8 |
| 7. Installed closure drift | R3.9 |
| 8. Trusted/supplemental race | R2.7, R2.12 |
| 9. Double launch, one persisted result | R2.1, R2.11 |
| 10. Finalized artifact overwrite | R9.1–R9.5 |
| 11. Activation without prior verifier | R1.4, R1.7 |
| 12. CI PASS without on-box artifact | R1.8 |
| 13. Python shadow modules | R6.3–R6.5 |
| 14. Fresh box and Python upgrade | R6.8, R6.9 |
| 15. Duplicate verdict keys | R4.3 |
| 16. Spec replacement | R4.1, R4.2 |
| 17. Exhaustive policy classification | R3.10–R3.13 |
| 18. Full compatibility sequence | R10.2, R10.3 |

### 8.9 Falsely-green mitigation checks

- SPEC-015 must enter the real production state machine; fixture-only adapter success is insufficient.
- Reviewer and PR spies are checked at the transition boundary.
- Real isolated invocation records are required; constructed aggregates are insufficient.
- Candidate-application forgery is tested separately from candidate-test substitution.
- `./scripts/test` PASS does not authorize activation without the on-box artifact.
- Dispatch 4 activation is decided by the frozen verifier, not its own policy.
- Extraction baselines are captured before candidate work and approval-bound.
- Metrics window tests use terminal-spec anchoring and complete history.
- Sanitizer tests assert syntactic containment only.
- SPEC-015 hash stability is not treated as proof of general evidence sealing; R9 and the separate evidence-hashing plan cover that distinction.

## 9. Rollback / irreversibility

1. Stop new dispatches before changing an active parent or activation manifest.
2. Roll back in reverse dependency order, preserving every attempt and review artifact.
3. An activated parent may be replaced only by another commit with a valid manifest chained to the current active commit.
4. Never edit or delete an activation manifest to reinterpret history. A revoked activation receives a separate operator record; the original remains evidence.
5. Dispatch 5 rollback must preserve v4 artifacts and display-only readers. Do not authorize them through v3.
6. Dispatch 4 rollback returns to the last activation-manifested parent and keeps Dispatch 0 installed. Do not restore exit-code-only authorization for new work.
7. Dispatch 3 rollback restores wrapper, helper, and migrated tests as one unit.
8. Dispatch 2 may be reverted independently, but Phase 2 immediately becomes blocked again.
9. Dispatch 1 rollback requires a high-assurance dispatcher change and approval-bound parity validation.
10. Dispatch 0 removal is allowed only after a native parent gate has been independently validated and no active or rollback path depends on its frozen profiles.
11. Installed snapshots and ABI/platform venvs may remain inert for rollback. Remove them only after proving no active unit or manifest references them.
12. Existing attempts, including SPEC-015, are irreversible evidence.
13. Full evidence-seal rollback rules remain owned by the separate evidence-hashing plan.

## 10. Open questions / operator decisions

None within this plan.

The approval-schema plan is a declared prerequisite, not an unresolved choice. A compatibility change that cannot pass the three-state sequence requires a new, separately scoped operator decision and is not authorized here.

## 11. Provenance

- **Challenge (Claude):** Revision 2 challenged with sustained/scaled dispositions for B1–B12, operational defects, Assumption 4, and validation gaps, dated 2026-07-14.
- **Drafter response:** Revision 3 applies the disposition table exactly, including its scoped reductions and recorded residual risks.
- **B11 routing:** Option (a), dependency on the separate approval-schema plan, selected because weakening exact bootstrap and changed-test bindings would contradict the high-assurance objective.
- **Dual validation:** Fresh-context SOL review of revision 3 pending. Claude review of revision 3 pending. Any unresolved blocker requires another revision.
- **Authorization:** Pending authorized-revision digest, prerequisite approval-schema activation, Dispatch 0 artifact authorization, and per-dispatch approvals.
- **Completion reconciliation:** Pending; record followed, authorized deviation, or unauthorized deviation for all six dispatches.

## Residual risks (recorded)

1. **Minimal bootstrap coverage:** Dispatch 0 is not a full second production gate. Its frozen Dispatch 1–4 profiles may cover less behavior than the completed native gate. This is the accepted B1 scaling; independent human high-assurance review remains required.

2. **Candidate snapshot mutability:** Revision 3 mandates tree revalidation but does not require a read-only bind mount of the candidate tree. A read-only mount remains optional hardening. There remains a race window between checks, reduced by commit-object review inputs and the immediate pre-review/pre-PR validations.

3. **Semantic prompt injection:** JSON escaping and fixed delimiters provide syntactic integrity only. Candidate-controlled diff and log content may still semantically influence the reviewer. The reviewer is therefore non-authoritative for mechanically decidable invariants.

4. **Dependency supply-chain completeness:** Venv addressing includes requirements bytes and Python ABI/platform, but this plan does not add a fully hash-locked wheelhouse or prove all transitive artifact hashes. That remains deferred dependency/supply-chain work.

5. **Full evidence sealing:** Revision 3 provides no-replace creation, dispatcher-owned paths, and finalized-attempt refusal only. Complete artifact sealing, terminal manifests, directory durability, and comprehensive hash-chain verification remain assigned to the separate `evidence-hashing` plan.

## Disposition record (revision 3)

- **B1 — SUSTAINED, scaled.** Added Dispatch 0: a minimal operator-installed frozen verifier with frozen required-test/assertion profiles for Dispatches 1–4, trusted snapshot provisioning, and activation manifests. Dispatch 4 activation is decided by the prior verifier. The absence of a full second gate is recorded as a residual risk.

- **B2 — SUSTAINED.** Replaced positive assertion count with parent-owned exact expected-assertion-ID sets. Missing, unknown, or duplicate IDs fail. Every substantive TRUST assertion requires an induced-failure mutation or specific approved justification. Free-form `test_command` output is non-evidentiary; `true` never counts as testing.

- **B3 — SUSTAINED.** Candidate processes receive neither nonce nor event descriptor. Added environment scrubbing, FD closure before every candidate `exec`, controller-only event production, per-invocation nonces, and environment/FD/`/proc` forgery attacks.

- **B4 — SUSTAINED, scaled.** Added mandatory clean-status and tree-identity revalidation after tests, immediately before review, and immediately before PR creation. A read-only bind mount remains optional and is recorded as residual hardening.

- **B5 — SUSTAINED.** Added root-owned `/srv/codexwork/installed/<commit>` snapshots, a complete closure manifest, verified interpreter identities, descriptor-based execution, no reopening through candidate-writable paths, and concurrent file/directory/helper/fixture/interpreter swap tests.

- **B6 — PARTIAL as dispositioned.** Added only `O_EXCL`/no-replace creation for new attestation/review artifacts, dispatcher-owned non-worker-writable paths, pipe-based worker output, and refusal to reopen finalized attempts. Full sealing and evidence hashing are explicitly routed to the separate `evidence-hashing` plan and recorded as residual.

- **B7 — SUSTAINED.** Added distinct trusted/supplemental pipes, nonces, source-class bindings, dispatcher-owned aggregation, cross-namespace forgery rejection, and a single-launch side-effect counter.

- **B8 — SUSTAINED with claim narrowing.** R7 now promises syntactic prompt integrity only. Machine summaries are primary; log tail is secondary untrusted evidence. The reviewer remains a fail-closed advisory oracle. Added a 16 MiB capture-time raw-log cap. Semantic injection remains recorded as residual.

- **B9 — SUSTAINED, scaled.** Venv identity now includes requirements bytes plus Python ABI/platform; trusted Python uses `-I`, safe cwd, scrubbed variables, and verified module origins. Added shadow-module, fresh-box, Python-upgrade, recursive permission, and staged post-merge dependency tests. Full wheelhouse hash locking is recorded as residual.

- **B10 — SUSTAINED, scaled.** Defined canonical complete-tree path-map comparison covering modes, symlinks, types, submodules, `.gitmodules`, additions/deletions, rename sides, unknown root paths, untracked control-plane files, and dirty state. Added the nonempty-required-set-or-policy-error property.

- **B11 — SUSTAINED, routing option (a).** Declared the separate approval-schema plan a hard prerequisite. Exact dispatch, attempt, verifier, baseline, inventory, and changed-test blob bindings must use that schema. No unsupported approval claim remains in PLAN-003.

- **B12 — SUSTAINED.** Added duplicate-key and nonstandard-constant rejection. The exact digest-approved acceptance-criteria vector is persisted at launch and used thereafter. Added spec-path replacement and legacy-consumer inventory tests.

- **Operational — activation manifest: SUSTAINED.** “Merged and installed” is now a strict machine-checked manifest state with commit chain, snapshot, verifier, inventory, venv, approval, portable, and on-box bindings.

- **Operational — Phase 2 gating: SUSTAINED.** Phase 2 implementation requires all six Dispatches 0–5 complete and active. Metrics alone is explicitly insufficient.

- **Operational — compatibility sequence: SUSTAINED.** Replaced the incomplete two-stage procedure with three states: bridge plus transitional test, strict-new test promotion, then bridge removal.

- **Operational — exact commit chain: SUSTAINED.** Activation records previous active commit and exact integration tip and rejects unenumerated intervening commits.

- **Operational — fresh-box provisioning: SUSTAINED, scaled.** Extended the single `scripts/setup-worker-user.sh` path to cover worker UID, snapshot, venv, systemd properties, ACLs, and activation. CI is explicitly portable-only.

- **Assumption 4 — SUSTAINED.** Removed representative-per-idiom as sufficient proof for TRUST migration. Exact per-test assertion inventory and per-substantive-assertion mutation/justification now govern TRUST tests.

- **Eighteen missing acceptance tests — SUSTAINED.** Folded all eighteen into numbered R1–R10 criteria and mapped them explicitly in §8.8.

- **Falsely-green validation paths — SUSTAINED.** Required real-state-machine SPEC-015 replay with reviewer/PR spies, actual isolated invocation provenance, candidate-application forgery tests, independent on-box activation, prior-verifier validation of Dispatch 4, pre-candidate approval-bound parity baselines, terminal-time metrics anchoring with complete history, syntactic-only sanitizer claims, and separation from full evidence sealing.
tokens used
69,591
